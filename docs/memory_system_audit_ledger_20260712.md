# Verbal Sage Memory System Audit Ledger

Audit date: 2026-07-12

Status: architecture approved. Memory V1 foundation is deployed in parallel; legacy runtime remains live while V1 is tested in shadow mode.

## Target invariants

1. Factual memory is owned by the authenticated Supabase `user_id`, never by a Vantage, persona, browser cookie, or thread.
2. Persona/Vantage settings may affect rendering but do not own, partition, or change factual memory.
3. Every backend memory read and write enforces actor/owner equality; the frontend is not the sole security boundary.
4. No cross-user retrieval, mutation, export, feedback, or debug visibility is possible.
5. Raw evidence, governed facts, response preferences, and internal job state are separate stores or strictly separate schemas.
6. Raw evidence remains auditable. Promotion, correction, merge, contradiction, supersession, retirement, and deletion are traceable.
7. Retrieval selects the smallest relevant governed fact set. Unrelated available memory is not injected.
8. Facts carry explicit entities, relations, temporal validity, provenance, evidence, confidence, sensitivity, authority, and contradiction state.
9. Importance, repetition, recency, and answer-use are separate signals. Repetition does not automatically make a claim true.
10. Background jobs improve specificity and organization without silently rewriting history or weakening security.

## Confirmed architecture

- Qdrant `memory_raw` contains high-recall conversational/episodic evidence and legacy card/profile records.
- Postgres `vantage_card.card_head`, `card_revision`, `card_link`, and `card_signal` contain structured cards, revisions, provenance references, and use signals.
- Candidate extraction is read-only in `scripts/personal_event_inventory.py` and `scripts/profile_fact_inventory.py`.
- `scripts/review_promotion_plan.py` produces review actions and has a guarded single-card create path.
- `rag_engine/memory_v1_intent.py` now maps request classification to governed `memory_intent` without accepting RESSE `response_mode`, persona, or `vantage_id` as inputs. Existing governed-claim shadow routing and the preference/project lane share this adapter.
- The durable preference/project selector is deployed behind `MEMORY_V1_SPECIALIZED_SHADOW=1` for one Supabase UUID. It records owner-scoped, non-injecting `memory.retrieval_trace` diagnostics containing IDs, keys, counts, reason codes, and hashes but no query preview or selected prose.
- Controlled production probes selected exactly two music preferences for a music recommendation, selected exactly the proposed feedback-loop roadmap for a project-planning request, and skipped an unrelated Mac question. All probes returned empty inspect-only answers with no specialized text in the prompt. Durable claims, preferences, project rows, and outbox counts did not change.
- Four reviewed durable personal cards exist: DeeDee death, Monika caretaking context, Neko correction, and Helsing pet loss.
- `rag_engine/vantage_router.py` reads approved active durable personal cards, applies deterministic matching, injects selected summaries into the system prompt, and records `injected_into_prompt` signals.
- The Admin Memory Review and Memory Inspector are read-only observability surfaces.
- Normal frontend chat derives `user_id` from verified Supabase authentication and sends the same ID in the Brains actor header and request body.
- Brains requires an infrastructure service token on `/vantage/query`.

## Confirmed problems and risks

### Identity and security

- Qdrant was published on every host interface. On 2026-07-12 it was changed to `127.0.0.1:6333`; persisted collections recovered green with `memory_raw=570` and `memory_claim_v1=4`, and a Verbal Sage private-VPC probe timed out.
- Redis was published on every host interface. On 2026-07-12 it was changed to `127.0.0.1:6379`, its persisted volume was retained, and a Verbal Sage private-VPC probe timed out.
- The Brains deployment Compose file contains database credentials directly instead of using an external secret. Rotation and dependency-safe secret migration remain required.
- `/vantage/query` now requires exact UUID equality between `x-vs-actor-user-id` and request-body `user_id`. Production probes return 401 for missing actor, 403 for mismatch, and 200 for a matching authenticated actor.
- `/log` now requires the same exact actor/owner UUID equality before Postgres or Qdrant writes and no longer Vantage-remaps the owner for new transcript, identity, or raw-memory records.
- The deployed frontend currently sends a verified Supabase ID, but Brains lacks defense in depth against a buggy or compromised service-token holder.
- `resolve_canonical_user_id()` keys aliases by `(vantage_id, alias_user_id)`, so canonical account identity remains persona-dependent.
- Durable personal records use `vantage_id='user_global'` while embedding the actual user ID inside `topic_key`; ownership is not a constrained column.
- Live catalog data contains similar identity/background facts under multiple user IDs and Vantages. Whether these are old aliases or distinct accounts still requires verification; they must not be merged by content alone.

### Store and schema contamination

- One generic card schema mixes factual identity/background/project data, style preferences, personal events, corrections, audit records, and internal consolidation cursors.
- 108 card heads exist: 66 active and 42 inactive. Only four are reviewed durable personal cards.
- Active low-quality extractions include `pref/https` and a development instruction stored as `pref/next_likely_step`.
- Exact or near-exact identity/background/project facts are duplicated across Vantages and historical runs.
- Approval, sensitivity, use scope, and surface policy are mutable JSON fields rather than constrained governance columns.
- Provenance is attached at card level, not to individual claims.
- No first-class entities, typed relations, temporal bounds, contradiction sets, supersession, or evidence authority exist.
- `card_revision.prev_revision_id` is not an enforced foreign key, and the head duplicates mutable summary/payload state.
- `strength` plus reward/punishment signals are inherited from behavioral learning and are not yet separated from factual confidence or importance.
- The `/log` request path executes `CREATE TABLE IF NOT EXISTS` and multiple `ALTER TABLE` statements during ordinary message writes; schema management is mixed with runtime ingestion.
- Postgres transcript writes are called authoritative, but exceptions are swallowed. The request may continue to Qdrant and return success after a Postgres failure, allowing silent store divergence.
- The special `frontend/identity` path writes a legacy Qdrant `memory_card` and returns without an equivalent authoritative Postgres transcript write.
- An existing `vantage_fact` schema contains source, entity, predicate, claim, evidence, and contradiction tables, but none has an explicit owner column.
- `vantage_fact` currently contains 600 sources, 600 entities, 711 claims, 712 evidence rows, and 3 contradiction groups. Chat sources represent 22 stored user IDs.
- Fact extraction is Vantage-scoped through `source.metadata.vantage_id`; claims are scoped only indirectly through evidence links.
- Extracted subjects are document entities, and claims use a globally unique canonical key. This is unsuitable as the account-owned canonical memory store.
- Existing `vantage_fact.source` rows may be useful as migration evidence when `metadata.user_id` is a valid authenticated account ID. Existing extracted claims should not be trusted or migrated wholesale.

### Retrieval and prompt pollution

- Runtime durable-card matching is hard-coded keyword logic for the current few scenarios.
- The query loads at most 12 high-strength cards before relevance matching; relevant lower-ranked cards can become unreachable as the catalog grows.
- Runtime selection does not visibly enforce sensitivity, authority, temporal validity, contradiction status, or claim-level provenance.
- A `MEMORY_ARCHITECTURE` turn injected Helsing loss, Monika caretaking, and the Neko correction, demonstrating unrelated prompt pollution.
- Code comments and debug metadata still describe the selector as debug-only even though it performs live prompt injection.
- The prompt receives flattened card summaries rather than a compact verified relational subgraph.
- Signals prove selection/injection, not actual answer use, correctness, usefulness, or harm.
- Qdrant personal retrieval is user-filtered by `payload.user_id`, which is a useful store-level isolation control once the caller identity is trusted.
- Vantage filtering for episodic memory is disabled by default, consistent with account-global memory; legacy strict Vantage behavior remains behind environment flags.
- Ordinary raw user chat is directly eligible for answer-time prompt injection based primarily on embedding similarity. It has not passed factual extraction, evidence comparison, contradiction handling, or promotion governance.
- The default raw-memory score threshold is low (`0.20`), and the search over-fetches 40–80 candidates before returning `top_k`.
- Ranking adds response-format, tone, intent, and legacy gravity bonuses. These behavioral similarities can outrank factual relevance and should not determine truth-bearing memory retrieval.
- Test/prompt pollution is controlled by lists of literal markers rather than an explicit record class or ingestion policy.
- A separate `score_personal_hit()` feedback scorer remains in the module, while live retrieval uses different inline scoring logic; this is duplicate/possibly dead policy code.
- The route audit passes its assertions, but several assertions certify current behavior rather than architectural correctness.
- `MEMORY_ARCHITECTURE` intentionally retrieves two raw personal messages plus one corpus chunk and injects compressed excerpts; this is useful history but still treats conversation text as answer authority.
- Audit metadata reports `raw_personal_memory_allowed=false` while personal raw hits are retrieved and represented in the prompt for `MEMORY_ARCHITECTURE`; the policy vocabulary is internally inconsistent.
- Durable-card recall probes still inject four additional raw personal hits alongside the governed durable card, increasing prompt size and allowing weaker evidence to compete with the approved fact.
- A family-death recall question is classified as `GENERAL`, showing that intent routing and durable-card policy use overlapping but inconsistent heuristics.
- The FM conceptual audit injects 16 thread messages totaling about 30,232 characters while declaring compression required. Thread context can dominate the prompt even when the personal archive is suppressed.
- Preference cards remain enabled for every audited route, including `TECH` and `FM_CONCEPTUAL`; polluted or obsolete style preferences therefore have broad reach.

### Legacy Vantage/persona controls

- User memory was historically Vantage/persona-scoped; the intended architecture now rejects that ownership model.
- Vantage response controls, cookies, learned reward/punishment behavior, persona cards, derived gravity/VB-desire profiles, and user facts remain intermingled.
- User-controlled response preferences may remain, but backend/internal control parameters and factual governance should not be editable as persona sliders.
- Active preference/profile loaders search the requested Vantage, `user_global`, and every legacy Vantage (`default`, `RESSE`, `EVA`, `RILEY`, `MORGAN`) for each request. This compatibility fan-in preserves cross-Vantage duplication and legacy behavior.
- Preference cards are loaded on every normal Vantage prompt, capped at eight before post-query topic filtering. Ignored cards can consume the SQL limit and hide legitimate lower-ranked preferences.
- Preference/profile card retrieval checks status and broad use scope but not review status, sensitivity, contradiction state, temporal validity, or provenance.
- Profile cards default to inclusion for `GENERAL` turns unless a narrow suppression heuristic matches. Generic chat can therefore receive up to ten biography/project cards without a demonstrated need.
- Profile/preference deduplication uses full `kind:topic_key`; duplicates stored under different historical topic keys or conflicting values are not semantically reconciled.
- The normal Vantage path disables the full legacy persona block but still injects legacy user instructions, request overlays, preference cards, conditional profile cards, raw/corpus chunks, and the newer durable-card block through separate paths.
- `format_memory_chunks()` labels source collections but does not convey claim authority, evidence quality, contradiction status, or why each item was selected.

## Direction supported by evidence

- Do not keep layering filters onto the generic Vantage card system as the final design.
- Define an account-owned relational fact/evidence model with strict Supabase user ownership.
- Preserve `memory_raw` initially as immutable/high-recall evidence, but stop treating raw conversation similarity as sufficient authority for prompt injection.
- Use governed facts/relations as the normal answer-time source; retrieve evidence only when verification or detail requires it.
- Separate response preferences from factual memory and separate both from internal job state.
- Migrate only verified useful records. Compare the new retrieval path in shadow mode before retiring legacy reads.
- Keep the inspector read-only; introduce mutation only through explicit authenticated governance operations with an audit trail.

## Memory V1 implementation status

- The `memory` Postgres schema is account-owned by `owner_user_id uuid`, with forced row-level security and transaction-local `app.user_id` actor context.
- Evidence, entities, claims, claim/evidence links, relations, assessments, revisions, candidates, preferences, retrieval traces, and projection outbox are separated.
- Six governed claims now exist for the exact authenticated owner: DeeDee death, Monika caregiving context, Neko name correction, Helsing loss, Neko loss, and Dahlia loss. The Neko loss depends on the explicit Neko-not-Nemo correction claim. Legacy records were not changed.
- Candidate application is proposal-hash locked, extractor allowlisted for automatic approval, revisioned, and supersession-aware.
- Retrieval rechecks every Qdrant candidate under Postgres RLS, then applies status, validity, sensitivity, domain, intent, surface, explicit-recall, claim-count, and token-budget gates.
- The Qdrant V1 projection uses a separate `memory_claim_v1` collection. Payloads contain owner, claim ID, revision, and policy metadata only; personal prose stays in Postgres.
- Projection processing is owner-explicit, uses `FOR UPDATE SKIP LOCKED`, avoids external calls inside database transactions, and does not lose a concurrently refreshed outbox job.
- The isolated schema/store/retrieval/projection test suite passes. Live prompt routing has not been switched to V1.
- Exact actor/body UUID enforcement for `/log` and `/vantage/query` passes helper, route-level container, and production probes and is deployed at backend commit `18001ac`.
- Production Qdrant and Redis host ports are loopback-only. Redis was checkpointed before recreation. Durable post-change Qdrant snapshots with checksums are stored under `/home/ubuntu/brains/snapshots/20260712_memory_v1`; Brains health passed after recreation. The six-point `memory_claim_v1` snapshot is `memory_claim_v1-6878092170277446-2026-07-13-03-43-36.snapshot` (SHA-256 `695d1c317587ac3c6379d645d617cf0999abe1aef3b8d29b2cb24233a37000ff`).
- Allowlisted, non-injecting route shadow evaluation is deployed for owner `1240822d-ac9a-4096-95aa-e2b24d36ef50`. Controlled probes selected exactly one claim for name correction, family loss, pet loss, and caregiving context; technical and unrelated nutrition turns created no V1 trace.
- Controlled shadow probe latency was approximately 0.79–1.07 seconds per inspected turn. Before broader rollout, reuse the route's existing query embedding or otherwise remove the duplicate embedding request.
- The first ordinary-use shadow sample contained 11 user turns: six governed-domain turns each selected exactly one intended claim, four negative controls correctly created no V1 trace, and one implicit family-event query was initially missed. The miss (`What happened with my mom?`) is fixed and now selects the DeeDee claim without classifying unrelated questions about the user's mother.
- A legacy pet-loss answer used the explicitly corrected wrong name `Nemo` and combined several raw memories. The reviewed source was hash-locked and promoted transactionally into separate Neko and Dahlia loss claims; Helsing received the corroborating evidence. The pre-change Postgres backup is `/home/ubuntu/memory_data_pre_pet_normalization_20260712.sql` (SHA-256 `2e1f007fe7bc17c843de0911793952e130493115f6172688c62b9adbc52dca39`). The guarded migration is deployed at `c3f86ff`.
- Named pet-event routing is distinct from spelling correction routing at `9f845a2`. Structured entity gating is deployed at `5977da6`: entity hints are compared only with governed structured fields, never arbitrary prose. Generic pet recall selected Helsing, Neko, and Dahlia; named Neko and Dahlia probes each selected only their matching event; the spelling probe selected only the correction. Production trace IDs are `1c3111b0-191c-4940-9983-76ed63ae8b36`, `c2e5e0b3-e690-47d5-b3e4-6d0b62011b08`, `0cb40eb6-a74a-4c91-9a80-77dd249f8203`, and `122a7cf2-7532-4b8c-8ea6-e62a1df076cf`.
- The complete Docker-isolated Memory V1 suite passed after entity gating: idempotent schema application, forced-RLS tenant isolation, actor authentication, store and projection integrations, shadow policy, reviewed-pet normalization, schema dump, and guarded rollback. Production remained allowlisted and non-injecting.
- Route-scoped embedding reuse is deployed at backend commit `739e032`. Personal archive retrieval, corpus retrieval, and Memory V1 shadow retrieval now consume one protected copy of the same query vector instead of issuing separate OpenAI embedding requests. A production GENERAL-turn probe activated all three consumers and reported `provider_calls=1`, `consumer_reads=3`, HTTP 200, and 0.606 seconds; its non-injecting V1 trace is `03bbcd53-e579-4e5e-8c98-31de69a42905`.
- The 2026-07-13 ordinary-use sample added 23 authenticated user turns across three threads (18,421 characters). Raw capture retained all 23. Only one pre-deployment governed-domain turn created a V1 trace because the current shadow classifier intentionally recognizes only the four seeded domains. The sample contains durable biography, temporary nutrition/training state, project/product decisions, response-policy preferences, transient workflow discussion, an incomplete utterance, and pasted assistant-generated text. No item from this sample has been promoted automatically.
- A recent-macros question received a generic “cannot access your data” response even though nutrition is a structured application domain. This is a structured nutrition-routing/integration defect, not a Memory V1 retrieval failure.
- The isolated artifact-provenance schema is implemented at `d2ef9b1`. It separates canonical artifacts, occurrences, sections, endorsement events, and optional candidate source spans. All new tables use forced RLS and composite owner-scoped foreign keys. Artifact sections default to `retrieval_eligible=false` and `promotion_eligible=false`; the migration has not been applied to the live database.
- Hash-locked artifact planning and persistence code is implemented on the isolated branch at `1b9af0b`. Its manifest fixes the five source UUIDs and SHA-256 hashes; mismatches fail closed. Artifact, occurrence, section, and endorsement IDs are deterministic. Replays use atomic insert-or-ignore followed by exact stored-value verification. The tested executable report path opens a read-only Postgres transaction and exposes no apply option.
- The live-source dry run validated all five hashes and segmented 90,517 characters into 213 non-overlapping sections. The largest section is 1,289 characters. Mixed user wrappers were separated from assistant-authored bodies; the Fractal Monism log remained mixed and provisional. Retrieval-eligible sections, promotion-eligible sections, and Qdrant projections were all zero. Production `memory.artifact` remained uninstalled. The report is `MEMORY_V1_ARTIFACT_DRY_RUN_20260713.json` with SHA-256 `dbbfc9d0322e83efa32977573fe626faa0c51a674117b826355f03ef5460b8e3`.
- Production artifact cutover completed on 2026-07-13. The pre-change custom-format `memory` schema backup is `/home/ubuntu/memory_pre_artifact_ingest_20260713T123249Z.dump` (78,305 bytes, SHA-256 `eb479a95c90eb0fad97bf7195b9d2355953e56b1ca0b706378df69761eee35a5`) with a validated 142-entry restore catalog. Live commits are `0780370`, `2075273`, and `d55147b`.
- The five-artifact transaction committed exactly five artifacts, five occurrences, 213 sections, five endorsements, and five immutable source-evidence rows. Claims remained 6, candidates 7, and projection outbox rows 6. Replay inserted zero rows. The owner sees `5/213` through `brains_app`; a different UUID sees `0/0`. Every artifact is `review_only`, every section has retrieval and promotion disabled, no artifact Qdrant collection exists, `memory_claim_v1` remains green at six points, and Brains remains active.
- The first account-wide Evidence Ingest/Triage dry run now covers all 140 authenticated `frontend/chat:user` turns. It uses owner-scoped forced-RLS reads, stable `(created_at,id)` keyset pagination, exact content SHA-256 values, duplicate independence keys, and existing-evidence hash verification. The code has no apply mode or database, candidate, claim, Qdrant, or prompt write path.
- The final triage retained 78 turns as raw-only and 15 as temporary evidence-only; it recognized the five archive-only artifacts, routed two operational nutrition/training turns to structured adapters, left two compact project statements for project-evidence review, and required atomic splitting for 38 mixed or multi-sentence turns. One exact duplicate shares its first occurrence's independence key. No conversational turn was directly promoted.
- Live-data review caught and corrected three unsafe heuristics before freeze: family recall questions no longer become biography, correction wording inside a question no longer becomes a correction assertion, and passive nutrition/project mentions no longer route to structured user data. Current caregiving difficulty and yesterday-only workflow problems remain time-bounded evidence rather than durable claims. Project requests without question marks remain non-assertional.
- The final report is `MEMORY_V1_EVIDENCE_TRIAGE_DRY_RUN_20260713.json` (SHA-256 `53e62060bfca588eb1b391845353d448dd636023501c15a5eb816d24dbbf0ecf`). The complete Docker-isolated Memory V1 suite passed after the final rules. The advisory partial cursor index was not applied to production.
- RESSE Policy Phase 1–3 was verified separately in `/home/ubuntu/chat-memory-resse-policy-v0_1` at commit `5647fa64d8fdb647066cb500c8e6ed099a518b22`. Its runtime contract keeps `response_mode` separate from `memory_intent`, prohibits RESSE as a memory owner/filter/namespace/alias, preserves authenticated Supabase ownership, and excludes governed Memory V1 collections from FM corpus retrieval. No RESSE integration code or Memory V1 runtime was changed during that verification. The contract's temporary reference to `vantage_card user_global ownership` is a compatibility statement, not the target ownership model.
- The read-only atomic-span planner now processes all 38 split-required turns into 984 deterministic, non-overlapping spans with exact source offsets, per-span SHA-256 values, stable UUID5 identifiers, and complete source-coverage verification. Source-level structured-dump detection excludes 520 pasted/quoted spans; 20 questions are excluded; 267 spans remain context-only; 11 remain temporary evidence; 64 ambiguous compound spans require another split; and 102 atomic assertions are review candidates (59 biographical/correction claims, 18 beliefs or opinions, 11 preferences, and 14 project assertions). Beliefs remain explicitly distinguished from external facts.
- Live-data auditing added regression coverage for project-parent preference leakage, complete versus incomplete conditionals, direct questions with leading conjunctions, incomplete history fragments, stable self-description, uncertain personal history, subjective evaluations, multiple preferences, mixed history/evaluation clauses, and speech-to-text `want to`/`went to` ambiguity. The complete Docker-isolated Memory V1 suite passes. No evidence, candidates, claims, Qdrant points, prompts, or production schema were written. The final report is `MEMORY_V1_ATOMIC_SPAN_DRY_RUN_20260713.json` (SHA-256 `6117cb7f537692f7d6397048b980f8ef0f0891ebe4981e909a16bf6eaf969ec2`).
- The deterministic compound-span review resolves all 64 previously manual parents. Fifty-nine were split at explicit clause boundaries and five were accepted as coherent single propositions after secondary review. The result contains 187 exact child spans with complete parent coverage, inherited absolute source offsets, child SHA-256 values, and stable UUID5 identifiers. No manual child remains.
- Semantic review retained 122 ambiguous, hypothetical, dependent, malformed, or meta-conversational children as raw context only and excluded three direct questions. Sixty-two children remain review candidates: 33 biographical claims, 12 explicitly user-held beliefs, seven preferences, and ten project assertions. Per-child flags identify 28 canonical rewrites, 13 uncertainty qualifiers, eight sensitivity reviews, five voice-transcription reviews, three contextual-subject resolutions, and all 12 belief-not-external-fact cases. None is automatically promotable.
- The compound splitter's live-data audit corrected indirect-question false positives, temporal-clause over-splitting, adjacent-boundary precedence, speech-to-text fragments, hypothetical examples, and context-dependent pronouns. The complete Docker-isolated Memory V1 suite passes with the new regression test. No evidence, candidates, claims, Qdrant points, prompts, or production schema were written. The final report is `MEMORY_V1_COMPOUND_SPAN_DRY_RUN_20260713.json` (SHA-256 `54fa0d41a70703871e7bab21d7603317f210f0191d060d81a8a547143d02919d`).
- The reviewed-span evidence persistence planner is implemented on the isolated Memory V1 branch at `1699b8e`. It proposes 164 deterministic owner-scoped rows: 102 final atomic review spans and 62 compound children. Epistemic roles remain explicit: 92 user assertions, 30 user beliefs/opinions, 18 preferences, and 24 project assertions. Beliefs carry `belief_not_external_fact`; no evidence row is treated as verified truth.
- The live-source persistence dry run found 164 unique span hashes, 164 planned inserts, zero exact-content duplicates, and zero conflicts. Four existing full-turn evidence rows are retained as covering provenance for 27 spans rather than counted as independent corroboration. All rows are high sensitivity by default; eight flagged spans are restricted. Two complete replays were byte-identical. Production remained at 9 evidence rows, 7 candidates, 6 claims, 0 preferences, and 6 outbox rows. The complete isolated CI suite passed. The report is `MEMORY_V1_EVIDENCE_PERSISTENCE_DRY_RUN_20260713.json` (SHA-256 `e38b14b551688d52468ec318d3242866a3a68c9bfdb689e63acbe01a07a6d941`).
- Deterministic row review found and corrected three metadata gaps at isolated commit `8b72e41`: four additional mental-health/medication beliefs are now restricted, ten approximate autobiographical statements now require uncertainty qualifiers, and three inseparable event/opinion spans now use `mixed_user_assertion_and_belief`. The reviewed plan contains 89 user assertions, 30 user beliefs/opinions, three mixed assertion/belief rows, 18 preferences, and 24 project assertions. It has 12 restricted rows, 23 uncertainty-qualified rows, zero conflicts, and zero exact-content duplicates. Full isolated CI passed and two complete live-source replays were byte-identical. Production counts remained unchanged. The report is `MEMORY_V1_EVIDENCE_PERSISTENCE_REVIEWED_DRY_RUN_20260713.json` (SHA-256 `d122028cc07abc8d3d122ba9779f04514ec75c6be1996cf2256854189c1f642a`).
- The reviewed planner remains dry-run-only. Its separate controlled apply command is gated by a hash-locked authorization manifest, `--apply`, and an exact confirmation phrase. `brains_app` is now database-enforced SELECT/INSERT-only on evidence; direct UPDATE and DELETE are disabled.
- The isolated evidence lifecycle implementation at commit `cfd1231` now removes that gap in the candidate schema without changing production. It limits `brains_app` to `SELECT, INSERT` on evidence, blocks direct updates and physical deletes with a trigger, and exposes one owner-derived transition function owned by a no-login/non-bypass maintenance role. Redaction and deletion clear raw content while retaining a hash/provenance tombstone; every request creates an owner-scoped append-only audit event with dependency counts and a source-copy warning.
- Candidate creation, approval, and application now require active evidence under a database trigger that serializes against lifecycle transitions. The Postgres packet builder includes only active evidence references and rejects claims with `no_active_evidence`. Tombstoned external identities cannot be reinserted. The lifecycle migration passed double-application, forced-RLS, cross-owner, idempotency, immutable-ledger, rollback, store, retrieval, artifact, and projection tests in the disposable PostgreSQL 16 suite.
- Evidence lifecycle is not complete account erasure. `public.chat_log`, artifact bodies/sections, relational claim derivatives, Qdrant projections, and retained backups require a later coordinated owner-scoped erasure workflow. The design is `docs/memory_v1_evidence_lifecycle_design_20260713.md`; no migration has been applied to the live database.
- The production-based deployment rehearsal branch is `memory_v1_evidence_lifecycle_deploy` at `b2d7473` (`93cf947` implementation plus formatting-only `b2d7473`). It fast-forwarded cleanly from production `2071d23`. Its disposable suite passed, and a restored clone of the full production database also passed the lifecycle migration and SQL controls with the expected owner-scoped counts `9 evidence / 7 candidates / 6 claims / 6 outbox / 0 lifecycle events`.
- The validated pre-deployment backup is `/home/ubuntu/brains/snapshots/memory_pre_evidence_lifecycle_20260713T193119Z.dump` (72,228,998 bytes, mode `600`, SHA-256 `9c6aefec453ee95c3d0662757299f2a035e8a78a34ee4121a9f459d81709800c`). The first application-role dump attempt failed closed under forced RLS and was replaced by this validated administrative archive.
- Evidence lifecycle deployed to production at `b2d7473077ac5919b117b678f623d5898c11106e` on 2026-07-13 after explicit approval. The transactional production security test passed and rolled back all synthetic rows. `brains_app` now has evidence `SELECT/INSERT=true` and `UPDATE/DELETE=false`; it can read but cannot write the append-only audit ledger. The maintainer role is no-login, non-superuser, no-inherit, and non-bypass-RLS. Brains restarted healthy with no post-restart exceptions.
- Post-deployment state is unchanged at 9 active evidence rows, 7 active-evidence candidates, 6 claims, 6 outbox rows, 5 artifacts, 213 artifact sections, and 0 lifecycle events. No retrievable claim lacks active evidence. Qdrant `memory_claim_v1` remains green with 6 points.
- The isolated controlled evidence-apply path now freezes the 140-row source cohort through `(2026-07-13T11:24:55.273202Z, 084366e0-e4a3-43ea-9ee3-91931a1d5dfd)` at SHA-256 `dd7f3c9983e47aa7e9d214ef7ec8e6463dd3fbda6248c778271144afb46c5c33`. It separately locks the reviewed report, plan fingerprint, artifact manifest, owner, 164 expected evidence rows, and deterministic batch ID `15064e5d-8cd3-5611-9cbf-db177d24a3a0`.
- The apply transaction runs memory reads and writes as `brains_app` under forced owner RLS, uses one evidence insert statement, exact post-insert immutable comparison, and an append-only batch header plus 164 row ledger entries. It commits only if evidence increases by exactly 164 while candidates, claims, preferences, projection outbox, and lifecycle events remain unchanged. Replay is read-only and exact. No Qdrant, extraction, promotion, preference, or prompt code is invoked.
- Disposable PostgreSQL 16 CI passed idempotent audit-schema application, insert-only privileges, forced-RLS cross-owner isolation, table-owner append-only guards, controlled commit, exact replay, source-tamper failure, zero downstream writes, schema dump, and guarded rollback. The live seebx preflight then reproduced all 140 sources and 164 reviewed rows with database writes `0`.
- The append-only ingest-audit schema deployed to production at `78906f8729065eabf8204e303b4d20ad429bac9f` after a production-based disposable CI pass. The pre-change backup is `/home/ubuntu/brains/snapshots/memory_pre_evidence_ingest_audit_20260713T213116Z.dump` (72,459,031 bytes, mode `600`, 886 restore-catalog entries, SHA-256 `a7911d1707b4bc8cee4fc207ea09b8e75ce9178b58e01e7fec749c738de72c15`).
- The live transactional security test passed and rolled back all synthetic rows. Both audit tables have enabled+forced RLS; `brains_app` has SELECT/INSERT and lacks UPDATE/DELETE; append-only triggers are live. Production contains zero audit batch headers and zero audit rows.
- The post-install live preflight reproduced the locked 140-source cohort and all 164 reviewed evidence rows with database writes `0`. Every database control reported true. The production counts remained `9 evidence / 7 candidates / 6 claims / 0 preferences / 6 outbox / 0 lifecycle events`, and Brains remained active.
- The reviewed evidence batch was explicitly authorized at isolated commit `63627e9`; the only manifest change was `apply_authorized=false` to `true`. The authorized manifest SHA-256 is `0a67fdf84723609b999b95e88c4d2437bda19163fb9a7f0cc36d715d0f954f57`.
- The final pre-apply restore point is `/home/ubuntu/brains/snapshots/memory_pre_reviewed_evidence_apply_20260713T213746Z.dump` (72,484,006 bytes, mode `600`, 909 restore-catalog entries, SHA-256 `d82d2eaf0905773e4571ed5c7a519bc9d64ba03764c1f3e3f5a0e642b040291c`). The authorized zero-write preflight reported every control true and `ready_for_apply=true` before mutation.
- Batch `15064e5d-8cd3-5611-9cbf-db177d24a3a0` committed exactly 164 evidence inserts and zero reuses. Evidence increased from 9 to 173 while candidates remained 7, claims 6, preferences 0, outbox 6, and lifecycle events 0. The ledger contains one committed batch and 164 exact evidence links.
- All 164 batch rows are active: 152 high sensitivity and 12 restricted. Every row has retrieval, prompt use, and candidate creation disabled. Exact replay returned `verified_replay` with zero writes; a different actor saw zero batch headers and zero batch rows. Brains remained active and Qdrant `memory_claim_v1` remained green with six points.
- The separated candidate extraction/review planner is implemented on the isolated Memory V1 branch at `26e39df`. It reads the committed batch as `brains_app` in a repeatable-read, read-only transaction; verifies forced RLS, every evidence and ledger hash, the batch authorization locks, the exact metadata distributions, and zero pre-existing target records; and exposes no apply path, model call, database mutation, Qdrant write, or prompt injection.
- The live dry run produced exactly 164 deterministic, hash-locked review units in disjoint lanes: 122 governed claims, 18 response/life preferences, and 24 project-knowledge statements. It extracted, approved, and promoted zero records. Ninety-two units use standard review; 72 require belief, canonicalization, decomposition, restricted-domain/sensitivity, subject, transcription, or uncertainty review. The report is `MEMORY_V1_CANDIDATE_REVIEW_DRY_RUN_20260713.json` (SHA-256 `3b50ee0c20c6893806a424ebc85aed0e4f39b3f0d0d1158f47cdfa8242d22fb3`).
- Governed claims can later use the existing `memory.candidate` review path. Preferences require a separate hash-locked candidate/review staging layer before `memory.user_preference` can change. Project knowledge requires a first-class versioned candidate and durable schema with authority, document state, conflict, and supersession semantics; it must not be stored as personal claims, preferences, or profile cards.
- All 72 specialized review units were reviewed exactly once on the isolated Memory V1 branch at `5cba5fd`. The decision set contains 55 canonical rewrites, five required splits, seven context-deficient deferrals, and five rejections. It produces 66 draft statements but creates zero candidate or durable records. Forty-seven claim drafts require comparison against existing governed claims; three preference drafts remain blocked on preference staging; five project drafts remain blocked on the project-knowledge schema.
- Review corrected two epistemic classifications, rejected volatile/context-only/wrong-target spans, preserved uncertainty in dates and quantities, and kept all eight mental-health/treatment statements as restricted user-held beliefs rather than medical facts or advice. Third-party health, relationship, and allegation statements received explicit-recall or direct-relevance surface restrictions. `Luxemburg-Casco High School` spelling was normalized from the voice transcript using the institution's official site and recorded as external normalization provenance.
- The complete specialized review report is `MEMORY_V1_SPECIALIZED_REVIEW_20260713.json` (SHA-256 `0ec58b9a3db4e18c6716da5b48b22307add381244bda8eaf619a87e99bc03841`). The source and reviewed reports are mode `600` under `/home/ubuntu/memory-v1-reviews`. The user ratified all 72 classifications on 2026-07-13 as design input only; this does not authorize extraction, candidate or review persistence, promotion, retrieval, or prompt use.
- Separate preference and project-knowledge schemas are committed on the isolated Memory V1 branch at `9304c36`. Preference and project proposals have immutable owner-scoped candidate/evidence tables plus hash-bound append-only review ledgers. Project knowledge uses immutable heads, numbered revisions, evidence provenance, authority/document state/effective time, and explicit conflict/supersession relations. `brains_app` may append proposals but cannot review or apply them; active `memory.user_preference` is application read-only. No production migration or data write has occurred.
- The complete PostgreSQL 16 suite passes double-apply idempotence, forced-RLS cross-owner isolation, narrow grants, active-evidence guards at link and review time, stale-hash rejection, rewrite/split lineage guards, append-only protection, accepted-candidate and gapless project revision enforcement, review-bound content hashes, specialized lifecycle link counts, every existing integration test, schema dump, and guarded rollback. The design is `docs/memory_v1_preference_project_schema_design_20260713.md`.
- Controlled preference/project review and apply functions are committed on the isolated branch at `8175781`. Review order is database-assigned under owner/candidate locks; request IDs are hash-bound and exactly replayable; all linked evidence must remain active; and jobs cannot accept or reject. Apply requires the latest accepted review, permits one use of that acceptance, rejects stale expected revision IDs, copies evidence into immutable revisions, and appends owner-scoped audit events. Preference apply alone advances the controlled active snapshot; project apply advances a gapless same-head revision chain. Vantage identifiers are absent from ownership, keys, and filters.
- The full disposable PostgreSQL 16 suite passes migration reapplication, forced RLS, locked no-login function ownership, narrow application grants, exact review/apply replay, changed-request rejection, job authority limits, stale revision rejection, two-revision preference and project chains, unverified-project rejection, cross-owner denial, append-only protection, every prior Memory V1 integration, schema dump, and guarded rollback. The design is `docs/memory_v1_review_apply_design_20260713.md`. Production remains unchanged and the recent ordinary-use conversations were not extracted or promoted by this phase.
- Production-clone rehearsal passed for staging `9304c36` plus review/apply `8175781`. The validated source backup is `/home/ubuntu/brains/snapshots/memory_pre_preference_project_rehearsal_20260713T005247Z.dump` (72,922,800 bytes, mode `600`, SHA-256 `140a54c04f60d02cce93f0da33a5598f0ba3782b22bf2cea89bffe92b33bd4a7`). A separate Docker clone reproduced `173 evidence / 0 preferences / 6 claims / 7 candidates / 1 ingest batch / 5 artifacts / 213 artifact sections`.
- Both migrations applied twice on the restored production clone, both transactionally rolled-back SQL tests passed, all 18 new tables had enabled+forced RLS, all five API functions had locked maintainer ownership and fixed security configuration, direct application writes remained denied, and every new table was empty afterward. The schema-only artifact is `/home/ubuntu/memory-v1-reviews/MEMORY_V1_REVIEW_APPLY_PROD_CLONE_SCHEMA_20260713.sql` (216,279 bytes, mode `600`, SHA-256 `5a96c722a5f97bafee5d98a98a21254590479470662f98af6ce945ff4adb195a`). Both guarded rollbacks returned the clone from 39 to 21 Memory V1 tables without changing any baseline count, and the clone volume was removed.
- Final production verification remained `21` Memory V1 tables with every preference/project staging and review/apply table absent. Counts were unchanged, Brains was active, and Postgres, Qdrant, Redis, and the vetting worker remained running. The rehearsal record is `docs/memory_v1_review_apply_production_clone_rehearsal_20260713.md`; production installation still requires separate approval.
- Preference/project staging `9304c36` and controlled review/apply `8175781` were installed on production after separate authorization. The fresh restore point is `/home/ubuntu/brains/snapshots/memory_pre_preference_project_install_20260714T010425Z.dump` (72,942,172 bytes, mode `600`, 909 restore-catalog entries, SHA-256 `c45d2a58789808b43f659403a04a57958779e7e672b3f90d28348d25ac7d491d`). Both live SQL security suites passed inside rolled-back transactions.
- Production now has the expected `39` Memory V1 tables. All 18 new tables are empty with enabled+forced RLS; the locked review maintainer has no login or RLS bypass; `PUBLIC` has no new table or function access; only the five intended controlled entry points are executable by `brains_app`; and Vantage is absent from the new schema and controlled functions.
- Zero real-data change was verified by restoring the fresh pre-install archive into a temporary database and comparing row counts plus canonical row hashes for all 21 original Memory V1 tables. Every manifest entry matched. Final counts remain `173 evidence / 6 claims / 7 candidates / 0 preferences / 5 artifacts / 213 artifact sections`; all new preference/project rows are zero. The temporary database was removed, Brains remained active, all four supporting containers remained running, and the production Git worktree was untouched. The deployment record is `docs/memory_v1_review_apply_production_install_20260713.md`. Candidate extraction and persistence remain separately unauthorized.
- The hash-locked preference/project extraction dry run proposes exactly eight deterministic candidates and eight active-evidence links: three preference candidates and five Verbal Sage project candidates. Two context-deficient project units remain deferred. The two related music preferences retain separate provenance and share an explicit overlap-review group rather than being silently merged.
- The extractor runs as `brains_app` in a repeatable-read, read-only transaction and has no apply option. It verifies the specialized-review report (`0ec58b9a3db4e18c6716da5b48b22307add381244bda8eaf619a87e99bc03841`), extraction manifest (`25d59e6f777afb4cf37057278876d58af9b11502bded6c52f0a546261d7b2cee`), every evidence and ingest-ledger hash, active status, batch membership, forced RLS, and zero existing target rows. Changed inputs fail closed.
- Full disposable PostgreSQL 16 CI passed, and two live runs were byte-identical. Production before/after counts matched with zero candidate, review, project-space, durable preference, durable project, Qdrant, or prompt writes. The mode-600 report is `/home/ubuntu/memory-v1-reviews/MEMORY_V1_PREFERENCE_PROJECT_EXTRACTION_DRY_RUN_20260713.json` (20,526 bytes, SHA-256 `fce21afc06065b2b99c0fa11bde844da2eee158e21cd8ed90506c99345829728`). The design record is `docs/memory_v1_preference_project_extraction_dry_run_20260713.md`.
- The frozen extraction set was persisted and reviewed through the controlled production functions: three preference candidates plus three accepted reviews and five `verbal-sage` project candidates plus five accepted reviews. Exact replay was zero-write, cross-owner visibility was zero, and all durable tables remained empty until separately authorized.
- Durable apply committed exactly 32 rows at isolated commit `e08936a`: three preference heads/revisions/evidence links/events and five project heads/revisions/evidence links/events. The final restore point is `/home/ubuntu/brains/snapshots/memory_pre_durable_preference_project_apply_20260714T103201Z.dump` (74,359,967 bytes, SHA-256 `27a9bafc492540bba1db43b05e17fcbbe9530eda96a3477a37fc8dab1e1c892f`). Exact replay returned the same eight event IDs with zero writes; evidence, claims, outbox, lifecycle rows, Qdrant, prompts, and retrieval activation were unchanged.
- The preference/project shadow retrieval contract is implemented read-only on the isolated branch. Response preferences compile to zero-token backend controls and never enter content; life preferences require matching intent/domain; project knowledge requires exact project ownership/key, topic relevance, document-state rules, sensitivity limits, active evidence, and budgets. Unmodeled project relations and scan-limit overflow fail closed. `memory_intent` remains independent from RESSE `response_mode`.
- All 15 locked production dry-run cases passed with zero database, trace, Qdrant, prompt, or runtime writes. Music, response-control, roadmap, historical-status, AB-design, website-history, and FM-dataset cases selected only their expected records; vague, unrelated, wrong-project, and over-sensitivity cases selected none. Before/after counts stayed `3 preferences / 5 project records / 6 outbox / 30 retrieval traces`, and a different owner saw zero. Report SHA-256: `e73953554d0468f2a6f378aad4fc846ab96e3ecb8665c329fc209ec24722b0e2`.

## Historical long-artifact inventory

- Account-scoped `public.chat_log` contains 140 user turns across 27 threads from 2026-06-16 through 2026-07-13. Twenty-six turns are at least 1,200 characters. Keyword matching is only an inventory aid; it is not authority or promotion logic.
- Qdrant `memory_raw` contains 287 records for the same owner: 139 user turns, 140 assistant turns, six legacy memory cards, one gravity-daemon record, and one VB-desire-daemon record. It contains no additional conversational history predating the SQL capture boundary. The 63,285-character artifact `bc973ce8-2d20-423f-8fba-5897328ce7d3` exists in Postgres but has no matching Qdrant point, confirming one best-effort mirror failure; Postgres remains the source of record.
- `public.chat_messages` contains 68 legacy messages from 2025-10-31 through 2025-11-02, but its sessions use obsolete `user_hash` values. No deterministic mapping exists from those hashes to the current Supabase UUID or registered aliases. Those rows are excluded from account memory inspection and migration unless ownership is established separately.
- Confirmed artifact `d8e6467d-6ec6-405e-b03b-bf65752dcd06` (2026-07-02, 18,126 characters, SHA-256 `2fb29eb97b7ca6afdfd5cc0410d9ab33775b9ae11b9fcaa7eceda26c810cd79d`) is a pasted historical Memory System Checkpoint. Treat it as an assistant-authored historical reference, not current truth.
- Confirmed artifact `a1f0753d-6424-47d8-b30c-642b869b9116` (2026-07-02, 4,429 characters, SHA-256 `13bd7d92011d411accb4fce5fe647f8d2533de0023cce1f9bbae3d6c351b8c14`) is a relayed ChatGPT analysis of the memory roadmap. Treat it as assistant-authored reference material, not ratified policy.
- Confirmed artifact `0a4bcbe1-ecb8-40a7-b3d0-8af05ce50e28` (2026-07-08, 2,865 characters, SHA-256 `b3a1b024377f78e8dfd39daecb427d5aa53e80e3915a7167c031d6ba5a96aada`) is a pasted memory-system progress summary. Treat it as an assistant-authored historical checkpoint.
- Confirmed artifact `4e1d31ad-b1d0-4b41-9992-995820d93567` (2026-07-13, 1,812 characters, SHA-256 `e75786db10d7ce26f52714b5b032c987b34f141b60c36cef1ff41f8edd95c893`) contains an explicitly positive user wrapper plus a pasted assistant-authored outline. Preserve the mixed-message boundary; record reference endorsement without converting authorship or automatically ratifying every sentence.
- Confirmed artifact `bc973ce8-2d20-423f-8fba-5897328ce7d3` (2026-07-13, 63,285 characters, SHA-256 `f58c0bc4ed696011162f4ac7b0b672b24514620266410c92b6363a8531787219`) is a structured Fractal Monism working-philosophy and chat-guidance log. It is mixed-authorship and explicitly labels itself provisional, continuously revised, and non-canonical. Preserve the full document, segment it by its 134 headings, and require section-level review before retrieval or candidate extraction.
- Duplicate raw submissions must become additional artifact occurrences rather than duplicate canonical artifacts. For example, chat rows `1ff93f8e-68ae-40df-b2b1-5d71ce6ba0f9` and `980289f1-176c-4f6d-86d6-60312ff39507` have the same SHA-256 and should deduplicate if they enter the artifact pipeline.
- For this owner, submitting long material implies reference endorsement because the user explicitly stated that pasted material is ordinarily endorsed. It does not imply user authorship, current factual accuracy, or ratification as governing response policy. Explicit language is still required for `ratified` status.

## Unresolved checks

1. Inspect the raw `/log` write path and Qdrant personal retrieval filters on seebx.
2. Verify all Brains memory routes enforce actor/owner equality, not only service-token access.
3. Map every active prompt contributor: raw personal retrieval, profile cards, preference cards, overlays, gravity/VB desire, thread context, and durable personal cards.
4. Determine whether repeated user IDs are historical aliases for one account or truly distinct accounts without exposing or merging data.
5. Inspect live Qdrant payload identity fields, legacy fallbacks, source exclusions, and Vantage filters.
6. Inventory Supabase/Vantage cookie controls and classify each as user preference, admin diagnostic, internal policy, or obsolete.
7. Verify deletion/export semantics cover raw evidence, governed facts, revisions, links, signals, traces, and derived embeddings.
8. Collect ordinary-use specialized traces and audit false positives, false negatives, minimality, latency, and cross-lane contamination. At the 2026-07-14 deployment stop there were two controlled traces and zero ordinary-use traces.
9. Keep preference/project prompt injection and answer influence disabled until the ordinary-use trace audit is explicitly reviewed and approved. RESSE Phase 4 remains paused at this shared boundary.
10. Design coordinated account export/erasure across chat logs, artifacts, governed derivatives, Qdrant, and retained backups.
11. Continue reviewed governed-claim candidate extraction from the remaining evidence lane without mixing claims, preferences, project knowledge, or structured application data.
