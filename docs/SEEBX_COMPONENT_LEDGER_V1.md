# SeeBx component ledger v1

Status: cleanup control ledger; no retirement authority

Evidence date: 2026-08-17 America/Chicago (2026-08-18 UTC)

Source commit: `49f9e60cf4321c8e42c359845c1a62a8c987614d`

## Dispositions

- **KEEP**: correct capability and ownership boundary; tighten as needed.
- **CONSOLIDATE**: necessary behavior split among overlapping paths.
- **EXTRACT**: retain generic logic while removing retired-package coupling.
- **REBUILD**: useful product capability whose implementation is unsafe or
  obsolete.
- **DECIDE**: product requirement or build-versus-buy decision is unresolved.
- **ARCHIVE**: preserve immutable evidence outside the active repository.
- **RETIRE**: proven unused, but deletion still requires a separate manifest,
  backup, authorization, and verification.

## Runtime and platform

| Component | Verified current state | Target owner | Disposition | Gate |
|---|---|---|---|---|
| `app.py` composition root and platform HTTP boundary | Production remains at `49f9e60c`; the isolated candidate reduces `app.py` to 141 lines with zero direct route or middleware decorators. `seebx.core.http_boundary` owns request correlation, the fail-closed service-token wall, and sensitive no-store headers; `seebx.capabilities.operations.health_routes` owns `/healthz` and `/readyz`. FastAPI is the sole `/openapi.json` owner after removal of one later-registered, unreachable duplicate | `app.py` composition root plus core HTTP boundary and operations health router | CANONICAL OWNERSHIP COMPLETE IN CANDIDATE; PRODUCTION MOVE PENDING | Twenty focused platform/Zep retirement tests and all 1,061 backend tests pass. The intentional duplicate removal changes 141 route registrations/hash `5c44a22b47340603447bc786aa743f14855cc4e779a20734e106d0a612ef8457` to 140/hash `79f06546520a5f89ce7d97a8ee8e88be40865e44df41ffd6988a994c64c1b4dd`; public OpenAPI remains exactly SHA-256 `e891a3e0efcefada32a4c7f5aed751e7968810c25f106deb4232280b2c1890c4`. Preserve the paired frontend contract and run authenticated end-to-end proof before production deployment |
| Supabase actor verification | Candidate `a20e9634` separates request identity/owner binding into `seebx.core.identity` and `seebx.core.ownership`, and issuer/JWKS/JWT verification into `seebx.adapters.supabase`; candidate `63e2e45a` adds one owner-independent request-actor verifier and makes all ten active app-owned thread operations require a verified Supabase bearer/header binding or active owner-bound voice-session lease before PostgreSQL ownership lookup. Verbal Sage candidate `c25502b` forwards the already-verified original bearer through all active thread proxy calls, ownership preflights, and the inspector active-thread lookup; the service token remains an infrastructure wall only | core identity/ownership plus provider adapter | CANONICALIZED IN PAIRED CANDIDATES; PRODUCTION MOVE PENDING | Fifty-three backend identity/conversation tests and four thread contract checks pass; 31 frontend thread/security checks pass; candidate TypeScript diagnostics exactly match the existing frontend baseline; the backend retains 149 routes and exact OpenAPI SHA-256 `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362`. Deploy backend and frontend only as one rollback-bound compatibility unit after authenticated end-to-end proof |
| `memory_actor_auth_v1.py` | Batch 02 candidate reduces it to a compatibility re-export; live callers use `seebx.core.identity` directly | `seebx.core.identity` | EXTRACTED; compatibility RETIRE pending | Candidate `fb252149`; prove dormant callers migrated before deleting wrapper |
| Platform PostgreSQL connection | Production is live on `127.0.0.1:5432`, database `memory`. Candidate `2497b9e7` gives all 16 formerly application-owned raw and owner-scoped connection lifetimes, canonical UUID actor-session setup, close-on-success/failure, and readiness probing one `seebx.adapters.postgres.PostgresConnectionProvider`; `app.py` contains no direct `asyncpg.connect`, connection close, actor-session SQL, or query call | shared platform PostgreSQL connection/session adapter | APPLICATION CONNECTION LIFETIME CONSOLIDATED; OTHER ADAPTER-LOCAL CONNECTIONS AND PRODUCTION MOVE PENDING | Twenty-seven focused unittest checks and 14 static thread-contract functions pass. Owner-session SQL and readiness SQL exactly match baseline `2a9aafb4` with SHA-256 `cb78da1ce4ffe257139f4aa15e740d45d5edbd13a061c2f2299bb47ca1940ea2` and `822ae07d4783158bc1912bb623e5107cc9002d519e1143a9c200ed6ee18b6d0f`; changed Python compiles; all direct routes audit with zero direct SQL/connection calls; production/candidate API parity remains 149 routes with route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362`; still migrate only required schemas/functions and prove exact row/ACL parity |
| LifeSwitch PostgreSQL connection | Live on `127.0.0.1:55433`, database `lifeswitch` | LifeSwitch domain PostgreSQL | KEEP | Remove fallback to `POSTGRES_DSN`; retain isolated credentials |
| Redis container | `brains-redis-1` is loopback-only; 203 persistent keys use 30,416 bytes and all were idle for the full observed container uptime; no application commands, live connections, process environment, package, or active runtime client was found | none | READY FOR REVERSIBLE STOP APPROVAL | Execute `docs/SEEBX_REDIS_RETIREMENT_PLAN_V1.md`; preserve hashed persistence bytes and stop without deleting the container or bind-mounted data |
| Qdrant runtime surface in `app.py` | Imported/configured but no live caller; no service/container | none | RETIRE | Batch 01 removes code/health surface; later remove dependency/config after dormant artifacts are archived |
| OpenAI SDK client and chat adapters | Candidate `0a1a4ba2` moves the OpenAI-only SDK client, model allowlist, embeddings, and compatibility helpers to `seebx.adapters.openai`; candidate `7f68089f` moves the generic and current LifeSwitch request/response/Chat Completions contracts from `rag_engine` to `seebx.adapters.openai_chat` and `seebx.adapters.lifeswitch_openai_chat` with no wrappers; live runtime has no `VANTAGE_MODEL` setting | provider adapters | CANONICAL PLACEMENT COMPLETE; PRODUCTION MOVE PENDING | Key fingerprinting permits in-process credential rotation without plaintext cache keys; all changed Python compiles; the 754-test conversation/voice/usage set retains only two failures reproduced at pre-move `fb133121`; all 53 Zep tests and exact 149-route/OpenAPI parity pass; historical manifests retain the former paths unchanged |
| `requirements-ci.txt`, `pyproject.toml`, and the immutable runtime build gate | Candidate defines one exact 12-package runtime contract and a content-free dependency verifier. The root-only offline builder validates a clean fast-forward repository, verifies the exact 43-wheel Linux x86_64 CPython 3.12 wheelhouse by filename/size/SHA-256, installs with hashes and `--no-compile`, runs `pip check`, the 12-of-12 dependency preflight, and twelve import checks, removes generated bytecode, normalizes path-bearing text files to `/opt/lifeswitch/runtimes/<commit>/venv`, rebuilds every wheel `RECORD`, and atomically publishes a root-owned read-only environment plus deterministic archive with `activated=false`. The archive binds every regular file, directory, and internal relative symlink; rejects bytecode, unsafe entry types, escaping links, and credential paths; and leaves the live mutable venv unchanged | SeeBx application package and deployment controller | RELOCATABLE REPRODUCIBLE BUILD GATE PROVEN IN CANDIDATE; PRODUCTION ACTIVATION PENDING | At implementation commit `d346fed6b487cac1b47774fffcba515837275054`, two complete builds under differently sized temporary paths produced byte-identical 105,082,880-byte archives with SHA-256 `a42fcbf34d0791a325e31e42d80031662f0fba0291112639c12215bda3cf9095` and tree-manifest SHA-256 `a515bfa67deaf7bf2fe9bf07077715343778819ff87f075f26cc5fa5c7f987b5`. Each archive contains 3,021 files, 358 directories, one internal symlink, zero bytecode files, and 20 final-install-path bindings; the build removed 495 bytecode files, removed the corresponding 495 stale `RECORD` rows, and rebuilt 44 wheel indexes. Twenty-three focused runtime/package tests and the complete 1,113-test backend suite pass. Rebuild once more against the final documentation commit and bind that exact archive and receipt into the separately authorized paired deployment; never mutate the live venv in place |
| Atomic paired release package | Candidate `scripts/verify_atomic_release_package.py` and `ops/releases/seebx_atomic_release_package_v1.schema.json` require one 14-artifact content-addressed package binding verified full Git bundles for the clean remote-matched SeeBx/Verbal Sage histories, a source-bound standalone frontend build with direct archive inspection and no environment files, the immutable runtime archive plus receipt, the disposable migration package/receipt and recovery receipt, exact database backup, systemd/environment evidence, service/timer baselines, quiescence, and reverse-order rollback. The verifier streams and independently inventories the runtime archive; binds its commit-specific final install path and `uvicorn` shebang; and rejects traversal, duplicate paths, bytecode, unsafe entry types, escaping links, writable files, credential paths, resource-limit violations, or receipt drift. The package is structurally unable to authorize activation | deployment control plane | FINAL PROTECTED PACKAGE VERIFIED; CANDIDATE NOT ACTIVATED | The complete 1,140-test backend suite passes. The exact package binding backend `3807a75866f297d7db0176394fabb59463993abc` and frontend `705422ad1edd4fd4a7d7e1c8a1d6cf0053b869a0` is protected under `/var/backups/seebx-cleanup/paired-release-v1/seebx-paired-release-20260820t0247z`; release-package SHA-256 is `e75db25d1650478b7a8fafbe10856db323243857f51ccef5514087472e82eb03` and verification-receipt SHA-256 is `b4d039d900ef0208c19b8fcf896f52a54213fbd4a68729288c7041b31ae1c3b7`. A later documentation-only commit makes this package historical and requires regeneration before cutover. Production activation remains separately authorized |
| Git daily sync | Active timer/service root | operations | KEEP | Verify remote/branch policy and avoid competing writer paths |
| Legacy `eval_all_users.sh` cron | Exact Ubuntu cron line retired on 2026-08-19; before/after crontabs, evaluator/log hashes, authenticated health proof, and rollback receipt are preserved under `/var/backups/seebx-cleanup/20260819T141946Z-legacy-qdrant-cron-retirement/` | none | SOURCE RETIRED IN CANDIDATE; RUNTIME RETIRED WITH ROLLBACK EVIDENCE | Continue observation through the Zep cutover; scripts and logs remain preserved until the broader retirement batch |
| Clean backend cutover preflight | `scripts/verify_clean_backend_cutover.py` resolves function identities as integer OIDs, wraps every database read in a serializable read-only transaction, and can explicitly inspect the Ubuntu crontab while running under a bounded administrative audit identity | canonical read-only deployment and legacy-schema retirement readiness report | LIVE PREFLIGHT EXECUTED; PRODUCTION CORRECTLY NOT READY | The complete 1,140-test suite passes in the sealed runtime. The 2026-08-20 live audit proved transaction, cron, zero-job, schema-shape, backup, and restore gates, but correctly blocked deployment because the immutable runtime is not installed and the Zep outbox, Zep-backed clear functions, trigger retirement, and telemetry-owner migrations are not yet applied. The two external legacy references are exactly `chat_history_private.clear_history` and `chat_history_private.clear_message_tail`, both replaced by the already verified bound migration. Production migration, deployment, and schema retirement remain separate authorized operations |
| Legacy memory recovery evidence | `scripts/prepare_legacy_memory_retirement_recovery.py` | canonical snapshot-bound dump, exact row manifest, disposable restore, cleanup proof, and content-free receipt | PROTECTED PRODUCTION RECOVERY RUN COMPLETE | Full-database dump SHA-256 `0adc9bcaba1baee76b815aeb3000ce3c0685367648e976c138354145668afa15` and restore-receipt SHA-256 `1f5a117b4dd83176e34141340c9fd9eede8623f1319d8cfd260fbf5f37cb9e24` are bound into the passing disposable migration receipt and protected paired package; the disposable restore database was removed |
| Voice synthetic canary | Active hourly but failed because its request used a field intentionally rejected by the live TTS route | voice operations | KEEP + REPAIR | Candidate `e7005538` aligns it with server-owned `conversation_style`; deploy and run one bounded canary before clearing historical failure state |

## Conversation and memory

| Component | Verified current state | Target owner | Disposition | Gate |
|---|---|---|---|---|
| Conversation response router | Candidate `b56d0e84` moves the mounted route from `rag_engine/resse_response_router.py` to `seebx/capabilities/conversation/router.py`, moves its test module, and leaves no compatibility wrapper; the external route, request schema, operation ID, and summary remain byte-for-byte OpenAPI compatible | `capabilities.conversation` | CANONICAL PLACEMENT COMPLETE; ORCHESTRATOR CONSOLIDATION CONTINUES | 117 bounded tests have exactly the same two pre-existing legacy failures before and after; compilation plus exact 149-route and OpenAPI hash parity pass; next split provider/composition selection by declared role without changing the public API |
| Conversation composition | Candidate `5ed33124` moves both live implementations from `rag_engine/response_composition_root_v0_2.py` and `v0_4.py` to `seebx.capabilities.conversation.composition` and `.lifeswitch_composition`; the generic pipeline is `ConversationResponseComposer`, the domain stage is `LifeSwitchResponseStage`, and the full wrapper is `LifeSwitchConversationComposer`; candidate `7f68089f` places both OpenAI chat adapters under `seebx.adapters`; candidates `b9069fa7`, `6f6ef721`, `f7a5f7e2`, `f89319c8`, and `9438dc52` place prompt assembly, orchestration, policy, finalization, and inspection under the conversation capability; `6f1272a1` places the LifeSwitch response plan, binding, and provenance contracts there; no legacy wrapper remains | conversation capability | CANONICAL PLACEMENT + ROLE NAMES COMPLETE; DEPENDENCY EXTRACTION CONTINUES | Compilation passes; focused conversation tests and all 53 Zep tests retain behavior with exact 149-route/OpenAPI parity; next extract the remaining LifeSwitch context dependencies without changing response behavior |
| Conversation prompt assembly | Candidate `b9069fa7` moves the single 1,172-line pure prompt implementation from `rag_engine/prompt_assembler_v1.py` to `seebx.capabilities.conversation.prompt`, updates every active importer and path-verification test, and leaves no wrapper; provider/runtime side-effect exclusions remain enforced | conversation capability | CANONICAL PLACEMENT COMPLETE; PRODUCTION MOVE PENDING | 86 focused tests retain only two failures reproduced at pre-move `d78506ad`; the 787-test conversation/prompt/voice set retains only its two previously documented failures; all 53 Zep tests, changed-file compilation, and exact 149-route/OpenAPI parity pass; historical handoffs and frozen evidence retain the old path unchanged |
| Conversation orchestration and policy | Candidate `6f6ef721` moves the 886-line orchestration implementation from `rag_engine/response_orchestration_v0_2.py` to `seebx.capabilities.conversation.orchestration`; candidate `f7a5f7e2` moves the 1,518-line policy implementation from `rag_engine/response_policy_v0_2.py` to `seebx.capabilities.conversation.policy`; all active importers and path-sensitive tests were updated, no wrappers remain, and stale RESSE/unused ownership descriptions were corrected without changing versioned or hash-bound contract fields | conversation capability | CANONICAL PLACEMENT COMPLETE; PRODUCTION MOVE PENDING | The 83-test focused orchestration suite retains only its two baseline guided-reflection failures; the 382-test policy/dependency suite has the exact same result before and after the policy move; the 787-test broad suite retains only its documented baseline failures; all 53 Zep tests, changed-file compilation, and exact 149-route/OpenAPI parity pass |
| Conversation finalization | Candidate `f89319c8` moves generic V1 finalization to `seebx.capabilities.conversation.finalization` and independently governed LifeSwitch V3 finalization to `.lifeswitch_finalization`; every active importer is updated, the old paths are explicitly guarded absent, and no compatibility wrappers remain; the two versioned implementations stay separate so LifeSwitch binding/provenance cannot alter the generic contract | conversation capability | CANONICAL PLACEMENT COMPLETE; PRODUCTION MOVE PENDING | All 40 focused finalization, persistence, inspection, provenance, and composition tests pass before and after; all 53 Zep tests pass; changed files compile; production and candidate both expose exactly 149 routes with identical route and OpenAPI hashes; full historical discovery remains non-gating because its existing stale manifests and retired suites produce unrelated failures |
| Conversation inspection | Candidate `9438dc52` consolidates the V1/V2 generic inheritance chain into `seebx.capabilities.conversation.inspection` and moves LifeSwitch V4 to `.lifeswitch_inspection`; the unnecessary V2 module boundary and duplicate strict-model base are removed, all V1/V2/V4 wire versions and builders remain, every active importer is updated, and explicit guards keep all three legacy paths absent | conversation capability | CONSOLIDATED; PRODUCTION MOVE PENDING | The same 35 focused inspection/path tests pass before and after, with only the same stale test expecting retired `persist_finalized_response_v3`; all 53 Zep tests pass; changed files compile; production and candidate retain identical 149-route and OpenAPI hashes; 19 net lines are removed |
| LifeSwitch response contracts | Candidate `6f1272a1` moves the current hash-bound response plan, final-answer binding, and content-free provenance receipt from three version-named `rag_engine` modules to `seebx.capabilities.conversation.lifeswitch_plan`, `.lifeswitch_answer_binding`, and `.lifeswitch_provenance`; all active runtime, adapter, persistence, clone-test, and focused-test importers move with them, and explicit guards keep the three former paths absent | conversation capability | CANONICAL PLACEMENT COMPLETE; PRODUCTION MOVE PENDING | The exact same 60 focused tests pass before and after; all 53 Zep tests pass; changed Python compiles; production and candidate retain identical 149-route, route, and OpenAPI hashes; wire versions, model validators, manifest hashes, stored contract names, and historical Git evidence remain unchanged |
| LifeSwitch context preparation and restricted reads | Candidate `b3a6925b` separates the pure selection, validation, hashing, and preparation contract into `seebx.capabilities.conversation.lifeswitch_context`; restricted owner-bound PostgreSQL access into `seebx.adapters.lifeswitch_context_postgres`; and environment, pool, and lazy-session management into `seebx.adapters.lifeswitch_context_runtime`. Both mixed version-named `rag_engine` modules are removed without wrappers | conversation capability plus PostgreSQL/runtime adapters | CONTRACT/ADAPTER/RUNTIME SEPARATION COMPLETE; PRODUCTION MOVE PENDING | The exact 60 focused tests pass; all 53 Zep tests pass; changed Python compiles; all 11 SQL-bearing statements and the complete restricted-session class AST are unchanged; production and candidate retain identical 149-route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362` |
| Conversation usage persistence | Production has 671 content-free usage events through 2026-08-17; the table is forced-RLS and the live conversation router is its only Python writer. Candidate `c6285574` replaces two version-named writer entry points with one `seebx.adapters.usage_postgres.persist_openai_chat_usage`, deletes the unmounted admin aggregation/cursor subsystem, and removes three one-time clone-verification scripts with zero external callers; the former admin route was already retired and the frontend main branch retains only a stale permission-registry label | PostgreSQL usage adapter plus platform usage schema | CONSOLIDATED; PRODUCTION MOVE PENDING; FRONTEND LABEL RETIREMENT PENDING | Five direct writer tests, the actor-registry tests, 60 conversation/usage tests, all 53 Zep tests, compilation, and exact 149-route/OpenAPI parity pass; 164 lines replace 2,129 lines while preserving the table, records, writer/admin roles, forced RLS, migrations, append/idempotency/conflict behavior, and historical Git evidence |
| Obsolete LifeSwitch V1 generation | Candidate `7ae253e1` removes `openai_chat_request_v3.py`, `response_lifeswitch_integration_v1.py`, `lifeswitch_prompt_integration_v1.py`, and their two V1-only test modules; the immutable historical handoff remains, and the exclusive-cutover test keeps explicit anti-reintroduction names | none | RETIRED IN CANDIDATE; PRODUCTION MOVE PENDING | Static imports, mounted routes, operational files, services, and timers have zero callers; 1,460 lines are removed without a wrapper; current V2/V4 focused tests add no failures, all 53 Zep tests pass, the 679-test affected set has only two failures reproduced at pre-change `dc9ffbd0`, and exact 149-route/OpenAPI parity is retained |
| Conversation snapshot | Candidate `e89018de` removes the mixed `rag_engine/response_conversation_snapshot_v1.py` module and leaves no compatibility wrapper; immutable validation, budgets, and hash-bound construction are owned by `seebx.capabilities.conversation.snapshot`, while repeatable-read owner/thread SQL and transcript-attestation checks are owned by `seebx.adapters.conversation_snapshot` | conversation capability plus PostgreSQL adapter | CONTRACT/ADAPTER SEPARATION COMPLETE; PRODUCTION MOVE PENDING | 18 direct snapshot tests and 139 affected tests pass; all 45 current `test_zep*.py` tests pass; 36 changed Python files compile; all six normalized SQL statements retain SHA-256 `a8a50d6b4e3aec8b99f2aeed1f0a4128a77f72feddbac6196440cbffb708caa2`; snapshot JSON/hashes and the 149-route/OpenAPI surfaces exactly match production; the governed-provider module retains the same 10 stale baseline errors before and after |
| Zep provider/runtime | Production prompt mode is on; candidate `25be0d0a` gives `app.py` and the response router one `seebx.capabilities.conversation.zep_runtime` singleton/settings owner | conversation capability with a future narrow `adapters.zep` provider boundary | KEEP; SHARED RUNTIME EXTRACTED; PRODUCTION MOVE PENDING | 66 focused conversation/Zep/erasure/response tests, all 52 `test_zep*.py` tests, compilation, and exact 149-route/OpenAPI parity pass; three broader stale tests fail identically at pre-batch `dd701e14`; still prove authenticated owner/thread binding, outage, export/retention, deletion compensation, and provenance before deployment |
| Old successor-memory adapter | Live only for excluded/no-memory surfaces | no-memory context provider | EXTRACT then RETIRE | Replace with explicit `NoMemoryContext` contract |
| Governed-memory package | 56 files; 16 reachable through response/auth coupling, 40 dormant | none after extraction | EXTRACT then ARCHIVE/RETIRE | Zero live imports and migration/runtime callers; 10 provider-test errors at pre-adapter candidate `000fc2b2` still assert retired `memory_input`/`memory_application` fields and must be reconciled with retirement rather than treated as current adapter regressions |
| `memory_v1*` code and active-runtime manifest | 45 files/26,764 lines in `rag_engine` have no live roots; the so-called active-runtime manifest declares 39 `memory-v1-*` units, but the server now has zero installed `memory-v1-*` units and the manifest/verifier has only test/script consumers, with no live service or timer reference | none | ARCHIVE then RETIRE | Preserve the hash-bound manifest as historical evidence; retire it only through an explicit manifest after zero frontend and operational callers are reconfirmed; do not refresh its hashes to make obsolete architecture look current |
| Vantage code/routers and metadata | Verbal Sage candidate `ca2c7fe` deletes the uncalled inspector BFF and its `/vantage/query` dependency. The current SeeBx candidate batch removes `vantage_id` from thread and transcript inputs and stops all new conversation and telemetry writes to the legacy column. Live historical rows remain: 1,117 `chat_log`, 851 `telemetry_event`, and 1,046 `vantage_answer_trace`; the trace was last written 2026-07-21. Four routines in the already-retiring `memory` schema still contain the identifier. | none for active execution; immutable recovery archive for retained history | ACTIVE INPUTS AND WRITES RETIRED IN PAIRED CANDIDATES; HISTORICAL DATABASE RETIREMENT PENDING | Thirty-two focused backend tests, the complete 1,142-test backend suite, 360 frontend tests, and the isolated frontend production build pass. Before schema removal, create and restore-verify an exact backup, deploy the no-new-writes candidate, prove a zero-write soak interval, inventory the four legacy routines, and execute a non-cascading hash-bound migration. |
| RESSE policy/evaluation code and compatibility vocabulary | The current candidate uses product-neutral request, operation, configuration, log, moderation-adapter, and prompt-assembly names. All eight remaining legacy literal values exist only in `seebx.contracts.conversation_provenance`, beside the disjoint namespace for the paired writer cutover. No RESSE-named implementation path or Python import remains. The zero-reader eight-case assistant-preference compiler dataset moved from active `evals/` into the hash-bound historical archive; the separately active 52-case response-policy regression remains in place. Read-only production inventory found 133 historical RESSE source rows, 30 tagged rows, 201 retired Vantage values, and six telemetry probes. | one centralized historical identifier decoder plus immutable evaluation history | ACTIVE/DORMANT IMPLEMENTATION VOCABULARY RETIRED; ZERO-READER EVALUATION ARCHIVED; PERSISTED IDENTIFIERS CENTRALIZED; WRITER CUTOVER DESIGNED | Repository searches prove zero runtime/CI reader for the archived dataset and one explicit script reader for the retained response-policy regression. The complete 1,146-test backend suite, compilation, 140-route set, and OpenAPI SHA-256 `3c296c3e0e286b75287e28e9fb87443467f32fa4b3d296d352edfec861d2506e` pass. `SEEBX_RESSE_IDENTIFIER_MIGRATION_V1.md` controls the paired cutover; immutable historical rows are never renamed in place and no compatibility router or environment fallback is permitted. |
| Direct cards/Vantage routes in `app.py` | Backend candidate `acec9d10` removes all four conflict-only cards/Vantage routes after Verbal Sage candidate `e26d296b` removes the sole active `/api/identity` caller. Retained frontend access logs contain 1,034 identity-sync requests, including 70 HTTP 502 responses on 2026-08-18 and 2026-08-19 UTC | none | RETIRED IN PAIRED CANDIDATES; PRODUCTION MOVE PENDING | Ten focused frontend tests and ten backend retirement/erasure tests pass; deploy only as a paired rollback-bound change, then confirm zero retired path hits |
| Chat/thread routes | Candidate moves all ten active thread-lifecycle handlers, their four request contracts, actor/owner enforcement, active-thread selection, history projection, metadata updates, and automatic-title orchestration from the composition root into `seebx.capabilities.conversation.thread_routes`; `app.py` now includes one injected router and retains no thread-lifecycle handler or helper. Existing Supabase/voice-session authority and the paired Verbal Sage bearer forwarding remain unchanged | conversation capability | CANONICAL HTTP OWNER COMPLETE; PRODUCTION MOVE PENDING | Thirty-four focused thread/identity/history/persistence tests and the complete 1,036-test backend suite pass; changed Python compiles; `app.py` falls from 1,317 to 795 lines and from 20 to 10 direct routes; an archive-based before/after comparison against `e31f7dd4` reports zero removed and zero added route signatures, with 141 routes, synthetic-environment route hash `5c44a22b47340603447bc786aa743f14855cc4e779a20734e106d0a612ef8457`, and unchanged OpenAPI hash `e891a3e0efcefada32a4c7f5aed751e7968810c25f106deb4232280b2c1890c4`; deploy only with the paired frontend candidate and authenticated end-to-end proof |
| Conversation persistence, transcript integrity, and active-thread selection | Candidate `d247da49` makes `seebx.capabilities.conversation.persistence` a SQL-free validation contract; `seebx.adapters.conversation_persistence` owns the atomic response/search writes; `seebx.contracts.transcript_integrity` owns the pure hash-bound attestation; and `seebx.adapters.transcript_integrity` plus `.thread_selection` own their PostgreSQL effects. The two zero-runtime versioned persistence wrappers and their misleading test names are removed without wrappers; stored web source identifiers remain unchanged in `seebx.contracts.conversation` | conversation contracts plus PostgreSQL adapters | CONTRACT/ADAPTER SEPARATION COMPLETE; DUPLICATE WRAPPERS RETIRED; PRODUCTION MOVE PENDING | 68 focused tests, all 53 Zep tests, 67 search tests, and compilation pass; 17 security/effect AST nodes and all 15 normalized SQL statements are unchanged with SQL hash `e7206ecfd1b8885d835a9464603e29b6315568731688664b9089fe7db481287b`; production and candidate retain identical 149-route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362` |
| Chat history clear | Candidate moves the strict request contract, verified Supabase actor gate, refusal mapping, and HTTP handler from `app.py` into one injected `seebx.capabilities.conversation.erasure_routes` boundary; the existing service and PostgreSQL adapter remain unchanged | conversation erasure HTTP boundary plus conversation deletion service | HTTP OWNER CONSOLIDATED IN CANDIDATE; OUTBOX DECOUPLING AND PRODUCTION MOVE PENDING | Nineteen focused identity/erasure/retirement tests and the complete 1,037-test backend suite pass; changed Python compiles; `app.py` falls from 795 to 571 lines and from ten to four direct routes; all 141 route signatures retain hash `5c44a22b47340603447bc786aa743f14855cc4e779a20734e106d0a612ef8457` and OpenAPI retains SHA-256 `e891a3e0efcefada32a4c7f5aed751e7968810c25f106deb4232280b2c1890c4`. The shared runtime is missing declared `jsonschema==4.25.1`; seven unrelated Forms tests fail identically at baseline without it and pass when the pinned dependency is supplied from a temporary test-only directory |
| Full chat + Zep clear | Candidate shares the same injected HTTP boundary while retaining the existing Zep-first erasure barrier and PostgreSQL clear service | conversation erasure HTTP boundary plus conversation deletion service | KEEP; HTTP OWNER CONSOLIDATED IN CANDIDATE; PRODUCTION MOVE PENDING | Focused tests prove verified Supabase owner binding, authority failure before service access, Zep-first deletion, Zep-outage refusal before PostgreSQL, and idempotent retry; exact public route/OpenAPI parity is retained |
| Attachments | Candidate `988596bd` owns validation/rendering and response-time reads in the conversation capability/PostgreSQL adapter. Candidate `b1a6b3bd` moves all four CRUD endpoints, five CRUD statements, connection lifetime, and the shared strict JSON UUID type out of `app.py`. Candidate `7b6ed07e` moves both owner/thread-bound message-binding statements into the attachment adapter without changing transaction or replay/conflict semantics. Candidate `92745e3b` moves the complete ordered thread-history query—including trusted-web evidence and attachment metadata projection—to `adapters.conversation_history`; `app.py` now contains no `public.chat_attachments` SQL | conversation capability plus shared identifier contract and PostgreSQL attachment/history adapters | ATTACHMENT CONTRACT, RESPONSE READ, CRUD, MESSAGE-BINDING, AND THREAD-HISTORY DATABASE BOUNDARIES EXTRACTED; PRODUCTION MOVE PENDING | Sixteen focused history/attachment/active-thread tests pass. The normalized thread-history query exactly matches pre-batch SHA-256 `acd12e056b2834fa63e7626116d20a777d5b40fb715ee8e3ff7979aded42b748`; changed Python compiles; the candidate remains at 149 routes with route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362` |
| Thread metadata lifecycle | Candidate `6e1c6e51` moves eight owner-scoped statements for create, visible-list, manual rename, pin/unpin, archive, title-state read, title-transcript read, and compare-and-set automatic-title update into `seebx.adapters.conversation_threads`; candidate `c210e2d8` adds the owner-bound thread-authorization lookup to that same adapter; candidate `63e2e45a` requires independently verified request-actor authority before that lookup, paired with Verbal Sage bearer-forwarding candidate `c25502b`. Route validation, 404 behavior, response shapes, transaction scope for create-plus-active-selection, and model generation remain unchanged in the capability layer | conversation capability plus PostgreSQL thread adapter | THREAD DATABASE EFFECTS AND APPLICATION CONNECTION LIFETIME CONSOLIDATED; PRODUCTION MOVE PENDING | Fifty-three backend identity/conversation tests, four thread contract checks, and 31 frontend thread/security checks pass; candidate TypeScript diagnostics exactly match the frontend baseline. The original eight normalized statements exactly match candidate baseline `cbd8a5af`; the owner lookup exactly matches baseline `22fe578e` with SHA-256 `3b63b68642a47c1829d1cdf804ee8f87a225701ad62ca5cedc8f1cd9a4df5daf`; changed Python compiles; production/candidate API parity remains 149 routes with route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362` |
| User transcript persistence | Earlier candidates move all seven SQL statements, transaction ownership, connection lifetime, submission idempotency, and attachment binding into the canonical PostgreSQL adapter. The current batch moves the remaining `/log` authority, validation, tagging, error mapping, and response ownership into injected `seebx.capabilities.conversation.transcript_routes`; request correlation becomes shared core infrastructure and the duplicate root-level tagging implementation is consolidated | conversation transcript HTTP capability plus canonical PostgreSQL persistence adapter | HTTP, VALIDATION, TAGGING, TRANSACTION, AND CONNECTION OWNERSHIP CONSOLIDATED IN CANDIDATE; PRODUCTION MOVE PENDING | Thirty-six focused transcript/attachment/identity tests and the complete 1,047-test backend suite pass; 150 legacy-versus-candidate tagging comparisons across every trigger family, five source labels, and both normalization modes match exactly; changed Python compiles; the persistence adapter and its seven SQL statements are untouched; `app.py` falls from 571 to 274 lines and from four to three direct routes; all 141 route signatures retain SHA-256 `5c44a22b47340603447bc786aa743f14855cc4e779a20734e106d0a612ef8457` and OpenAPI retains SHA-256 `e891a3e0efcefada32a4c7f5aed751e7968810c25f106deb4232280b2c1890c4` |
| User export | Backend route returns retirement conflict and the visible frontend download control still points to it; retained access logs contain no export requests | privacy/export capability | REBUILD; DO NOT SILENTLY REMOVE | Define which active stores are in scope, then implement owner-scoped bounded export, audit, retention semantics, and frontend tests before changing the UI or route |

## Search, retrieval, and catalog

| Component | Verified current state | Target owner | Disposition | Gate |
|---|---|---|---|---|
| Trusted-health search profile | Candidate mounts `seebx.capabilities.search.trusted_health`; provider, policy, registry, evidence, admission, and audit have one canonical owner | search capability | KEEP | Preserve approved domains, NCBI/ODS behavior, source evidence, and audit semantics through deployment verification |
| Search executor and shared manifest contract | Candidate owns plan, budget, authorization, and execution under `seebx.capabilities.search`; `bed831f5` moves the server-owned hashed `SearchCapabilityManifestV1` from `rag_engine` to `seebx.contracts.search` with no wrapper; shared language, OpenAI, transcript persistence, request identity, and voice-session authority use their canonical owners | search capability plus shared contracts | KEEP; SHARED BOUNDARIES AND CONTRACT EXTRACTED | 93 affected tests, compilation, a no-wrapper regression test, and exact 149-route/OpenAPI parity pass; preserve manifest bytes, server-owned budgets, and authenticated owner behavior through deployment tests |
| Search PostgreSQL effects | Candidate `7ffb6b82` leaves only content-free query hashing in `seebx.capabilities.search.audit`, keeps SQL-free settings/errors/HTTP behavior in `.runtime`, and moves the audit connection lifecycle, shared actor rate limit, audit start/finish, and metadata-only source evidence writes to `seebx.adapters.search_audit`; candidate `b2b6311f` moves persisted-search connection ownership to `seebx.adapters.search_transcript` and the active/unexpired ODS cache read to `seebx.adapters.search_cache`. The complete `seebx.capabilities.search` package now contains no direct PostgreSQL import, connection, fetch, or execution effect | search capability plus PostgreSQL search adapters | CONTRACT/ADAPTER SEPARATION COMPLETE; PRODUCTION MOVE PENDING | 147 search tests, all 53 Zep tests, focused endpoint/adapter failure and cleanup tests, and compilation pass; the normalized ODS query is unchanged with SQL hash `70c806103d1901fce7230535b38502cb9414e7d0b747c1692700a023abc4414f`; production and candidate retain identical 149-route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362` |
| Current-news search profile | Candidate expresses current news through the same provider, registry, evidence, admission, audit, and runtime boundaries | search capability policy profile | KEEP | Preserve freshness/source policy and exact public contract; do not recreate a separate engine |
| Legacy search-owned `rag_engine` modules | Candidate removes or moves 15 router, authorization, plan, budget, evidence, audit, provider, policy, registry, contract, NCBI, and ODS modules; `bed831f5` removes the final shared manifest path and no candidate caller imports it | none | RETIRE CANDIDATE COMPLETE | Keep Git rollback; verify deployment import graph, routes, authenticated behavior, ODS timer, and stored audit records before production retirement is accepted |
| Legacy RAG/vector modules | Mostly dormant with Qdrant/Vantage dependencies | none or offline evaluation | ARCHIVE then RETIRE | Generated import graph and no runtime settings/services |
| `seebx/capabilities/catalog/routes.py` | Live against old platform `catalog_dev` | catalog capability in LifeSwitch database | MIGRATE + CONSOLIDATE | Repoint after row/function parity; do not keep two writable catalogs |
| LifeSwitch nutrition catalog access | Live against isolated database `catalog_dev` | same catalog capability | KEEP | Becomes sole catalog after callers cut over |

## LifeSwitch domain capabilities

| Component | Verified current state | Target owner | Disposition | Gate |
|---|---|---|---|---|
| Nutrition router | Thirty-one retained routes are owned by explicit Log, Meals, Foods/Servings/Overrides, and Meal Plans modules. The former 810-line mixed food/meal-plan router is now a 48-line composition root that preserves the established registration order. Named PostgreSQL adapters own every Nutrition database effect; capability modules retain identity, validation, owner authority, USDA/provider policy, serialization, and HTTP mapping | `capabilities.nutrition` aggregate modules plus isolated PostgreSQL adapters | KEEP; DATABASE EFFECTS AND LOGICAL MODULE OWNERSHIP SEPARATED; PRODUCTION MOVE PENDING | All 17 moved handler ASTs, all 31 Nutrition routes, the complete 138-route table, and full OpenAPI document match the parent exactly. Forty-eight focused backend tests and all 1,331 backend tests pass. The paired frontend candidate also passes 12 Plan-profile/four-macro scoring tests, and the backend Plan projection test returns calories, protein, carbs, fat, and Plan targets |
| Training router | Forty-one retained routes are owned by explicit Exercises, Conditioning, Sharing, Templates, and Sessions modules. Delegated view-target policy is isolated in `training.access`, shared value validation is isolated in `training.common`, and the former mixed 1,430-line module is now a 78-line composition root preserving the established registration order. Named PostgreSQL adapters continue to own every Training database effect; capability modules retain identity, owner/delegation policy, token policy, validation, normalization, serialization, immutable-child retirement, and HTTP mapping | `capabilities.training` aggregate modules plus aggregate-specific PostgreSQL adapters | KEEP; DATABASE EFFECTS AND LOGICAL MODULE OWNERSHIP SEPARATED; PRODUCTION MOVE PENDING | All moved handler ASTs, all 41 Training routes, the complete 138-route table, and full OpenAPI document match the parent exactly. Training route SHA-256 is `d5ed7c79dbb71fd0fac1177c759692ed6afa243057dd003b03ec7c5f90b683c1`; route-table SHA-256 is `35ed7eb9b2c30688234135d182a2a45ea29111904b6149c747265e25e22450d5`; OpenAPI SHA-256 is `0c98736818a52d53797460c1f207f014d9882af64400363ae1fd69c21f54b46a`. All 103 focused Training tests and all 1,334 backend tests pass |
| Measurements | Three active owner-bound routes retain their exact wire contract. Candidate moves all four SQL effects, delegated People-permission query, and isolated LifeSwitch connection lifetime into `seebx.adapters.lifeswitch_measurements_postgres`; capability code retains identity, validation, delegation policy, serialization, and HTTP mapping. The configurable People schema is now a strict lowercase SQL identifier and fails before connection on invalid input | measurements capability plus replaceable isolated PostgreSQL adapter | KEEP; DATABASE EFFECTS SEPARATED IN CANDIDATE; PRODUCTION MOVE PENDING | 20 focused Measurements/identity tests and all 1,224 backend tests pass; parent/candidate route and OpenAPI hashes match exactly. Preserve frontend BFF contracts and prove authenticated owner/delegated behavior before deployment |
| Plan/domain context | Live across multiple readers/adapters | `capabilities.plans` | CONSOLIDATE | One read model and one write authority; retire legacy fallback |
| LifeSwitch snapshots | Installed schema; currently empty | plans/recovery if required | DECIDE | Prove product caller and retention need |
| Plan observation PostgreSQL adapter | Candidate moves the only live Python implementation from the misleading top-level `lifeswitch_agentic` package to `seebx.adapters.plan_observation_postgres`; the database schema name remains unchanged, the one runtime importer and path-sensitive tests use the canonical adapter, packaging no longer includes the obsolete Python namespace, and no compatibility wrapper remains | plans read model plus PostgreSQL adapter | CANONICAL PLACEMENT COMPLETE IN CANDIDATE; PRODUCTION MOVE PENDING | The moved 916-line implementation is byte-identical at SHA-256 `72e73acc500060343d9c9de2aa4994f0a2c049ed06545933fb617e7a64409eab`; the renamed direct test differs only by its canonical import; old Python imports are zero; 49 focused tests and all 1,152 backend tests pass; changed Python compiles; the candidate remains at 140 routes and OpenAPI SHA-256 `3c296c3e0e286b75287e28e9fb87443467f32fa4b3d296d352edfec861d2506e` |
| Nutrition batch creation | The canonical `/lifeswitch/nutrition/log/entries/batch` route is atomic, owner-bound, serving-aware, and capped at 100 items; the separate `/log/entries` implementation performs partial writes without a transaction, does not owner-bind its food lookup, has no application caller, and has zero retained frontend access-log hits while the canonical route has successful calls | nutrition log capability | LEGACY ROUTE RETIRED IN PAIRED CANDIDATES; PRODUCTION MOVE PENDING | SeeBx removes only the obsolete router module/import/mount; Verbal Sage removes only its unused proxy. Ten focused backend tests, two direct frontend guards, all 1,154 backend tests, all 365 frontend tests, backend compilation, and the isolated frontend production build pass. The SeeBx candidate intentionally changes from 140 to 139 routes and to OpenAPI SHA-256 `9955d2ba9a22ab1a60a91d31325a9900bceeee5e0c20b123f35d6039aeaa70ae`; explicit tests keep both legacy surfaces absent and preserve the canonical route |

## Preferences, forms, administration, and archive

| Component | Verified current state | Target owner | Disposition | Gate |
|---|---|---|---|---|
| Assistant response preferences | Frontend has four authenticated BFF calls; production backend is unmounted; the cleanup candidate rebuilds the exact surface under `seebx.capabilities.preferences` with independent Supabase verification, generic non-reflective validation errors, no-store responses, direct owner-scoped PostgreSQL, revision checks, typed compilation, explicit approval, and one hash-bound projection into the canonical response assembler. Only four enums and approved fixed rule IDs can influence presentation; raw narrative/profile text, Zep, RAG, tools, and external actions remain excluded | preferences capability plus typed conversation assembler | RETAIN; CANONICAL CAPABILITY AND RESPONSE PROJECTION COMPLETE IN CANDIDATE; PRODUCTION MOVE PENDING | Disposable schema/RLS/row-preservation proof, full-suite/default-prompt/owner/tamper/no-raw-text verification, then paired authenticated frontend and response verification |
| `user_settings` schema | Two forced-RLS tables contain exactly one preference row and nineteen compilation candidate rows; they are separate from governed memory | platform preferences | RETAIN + MIGRATE INTACT | Use the squashed clean-install baseline; preserve all twenty rows; never expose through the Supabase Data API |
| Forms capability | Candidate rebuild uses `seebx.capabilities.forms`, independently verified Supabase/voice identity, forced-RLS `lifeswitch_forms` tables, composite owner foreign keys, and a default-off feature flag. This batch moves all 15 SQL/transaction effects and connection lifetime into `seebx.adapters.lifeswitch_forms_postgres`; the six HTTP routes retain only identity, validation, response, and stable error mapping | forms capability plus replaceable isolated PostgreSQL adapter | REBUILT; DATABASE EFFECTS SEPARATED; PAIRED FRONTEND AUTH FORWARDING AND PRODUCTION MOVE PENDING | 27 focused Forms/migration tests and the complete 1,211-test backend suite pass; capability database effects are zero and parent/candidate route plus OpenAPI hashes match exactly under the same environment. Apply and verify schema only in a disposable restore, migrate only valid UUID-owned legacy rows, quarantine invalid-owner rows, forward the original bearer through the authenticated BFF, then separately approve activation |
| Frontend forms surfaces | Six authenticated BFF routes and active Forms Builder, Capture, and SSLG callers retain the existing wire contract | forms capability | KEEP | Enable only after owner-bound data migration and authenticated end-to-end verification |
| Telemetry router | Live and large/custom | observability capability | CONSOLIDATE behind OpenTelemetry contract | Define redacted trace/request/job correlation; select storage/alert backend separately |
| AI operations incidents | Candidate gives all three admin routes one operations-owned router and independently verifies the forwarded Supabase bearer against the asserted actor before honoring the server-authorized capability; four incident tables remain empty | operations | CANONICAL ROUTE OWNER + IDENTITY BOUNDARY COMPLETE; PAIRED FRONTEND FORWARDING AND PRODUCTION MOVE PENDING | Nine focused tests pass; route count, path count, route hash `2d0c3c3f9bd2a7c222a836d3c6fe9fa91413a85d0d193cb020ecb46f8de967dd`, and OpenAPI hash `e891a3e0efcefada32a4c7f5aed751e7968810c25f106deb4232280b2c1890c4` exactly match the pre-move candidate; prove the paired frontend bearer path before deployment |
| Admin memory workbench/health routes | Candidate `acec9d10` removes all four mounted retirement responses after repository and retained-log audits found no callers | none | RETIRED IN CANDIDATE; PRODUCTION MOVE PENDING | Backend retirement/erasure tests pass; retain Git rollback and confirm zero path hits after paired deployment |
| Archive/document library | Separate planned capability; not chat memory | archive capability | DECIDE | Product evaluation for storage, extraction, mapping, export, and deletion |

## Voice

| Component | Verified current state | Target owner | Disposition | Gate |
|---|---|---|---|---|
| Voice session lease | Candidate `381704fb` removes the legacy combined router: `seebx.core.voice_identity` owns request/session authority, `seebx.adapters.voice_session` owns PostgreSQL lease operations, and `seebx.capabilities.voice.session` owns the unchanged HTTP contract | voice capability | EXTRACTED; PRODUCTION MOVE PENDING | 64 full voice tests, 41 authority-reference tests, 298 search/trusted-web tests, compilation, exact 149-route/OpenAPI parity, and all eight normalized lease SQL statements pass/match |
| Voice-language contract | Candidate moves the shared catalog, validation, request header, response instruction, and transcription prompt from `rag_engine` to `seebx.contracts.voice_language` | shared contracts | EXTRACTED; PRODUCTION MOVE PENDING | Candidate `f8b73bf1`; 111 direct voice/response tests, 154 search tests, 62 voice tests, and exact route/OpenAPI parity pass; production still requires reviewed deployment |
| Shared voice observability and realtime preview state | Candidate `92adf9e3` moves content-free request correlation to `seebx.core.voice_observability` and the provider-free in-memory preview registry/state machine to `seebx.capabilities.voice.realtime_session`; every active importer uses the canonical owner and neither legacy path remains as a wrapper | core observability plus voice capability state | CANONICAL PLACEMENT COMPLETE; PRODUCTION MOVE PENDING | Both moved source files are byte-identical with hashes `7452a22d1cff711c256adf5f852b9a345a9bdf44a43519a2f45b0ddbdbaa49ca` and `6926ebb7240e033533f3fdc80abdca4f99bd261f60ed1e1b7b6174862b42e995`; 64 focused tests, all 66 `test_voice*.py` tests, compilation, and exact 149-route/OpenAPI parity pass |
| Voice transcription | Candidate `f3889883` mounts `seebx.capabilities.voice.transcription` directly with no legacy wrapper; owner/lease authority, audio/language limits, confidence calculation, and public HTTP semantics remain capability-owned, while `seebx.adapters.openai_transcription` owns model/URL/key configuration, safety-identified OpenAI HTTP, timeout/network handling, provider request identity, and response decoding | voice transcription capability plus OpenAI transcription adapter | CAPABILITY/PROVIDER SEPARATION COMPLETE; PRODUCTION MOVE PENDING | 17 focused tests and all 72 `test_voice*.py` tests pass; changed Python compiles; pure helper and provider-configuration ASTs are exact; production and candidate retain identical 149-route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362` |
| Voice synthesis/TTS | Candidate `03a4657f` mounts `seebx.capabilities.voice.synthesis` directly with no legacy wrapper; actor/session authority, request/text limits, server-owned model/voice/style, media contract, and public error mapping remain capability-owned, while `seebx.adapters.openai_tts` owns URL/key/default-voice configuration, safety-identified OpenAI HTTP, request payload, provider identity, streaming-resource lifetime, and provider failures | voice synthesis capability plus OpenAI TTS adapter | CAPABILITY/PROVIDER SEPARATION COMPLETE; PRODUCTION MOVE PENDING | 19 focused tests and all 79 `test_voice*.py` tests pass; changed Python compiles; pure capability and provider-configuration ASTs are exact against the former source hash `13c4198f51abe71b77f1c7ba02c70cd7444f1c6b1b50de9203e0494809e9f364`; success, upstream-error, and timeout cleanup are directly tested; production and candidate retain identical 149-route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362` |
| Realtime voice preview | Candidate `c9a552df` mounts `seebx.capabilities.voice.realtime_preview` directly with no legacy router wrapper; owner/lease checks, SDP limits, session lifecycle, commit/events/delete semantics, and public error mapping remain capability-owned; `seebx.adapters.openai_realtime` owns provider negotiation and `seebx.adapters.voice_realtime_config` owns secret loading. Candidate `64cd487e` moves the sideband controller to `seebx.capabilities.voice.realtime_sideband` and gives `seebx.adapters.openai_realtime_sideband` sole ownership of OpenAI WebSocket effects. Candidate `fd360921` gives the same-process loopback URL, client lifetime, timeout policy, three exact governed-turn paths, and HTTP exception translation to `seebx.adapters.voice_governed_turns`; the capability retains payload, authority, ordering, persistence validation, and public error semantics but no longer performs direct network effects | voice realtime capability plus OpenAI realtime/config/sideband and governed-turn transport adapters | PROVIDER EFFECT SEPARATION COMPLETE; GOVERNED LOOPBACK CONTAINED; DIRECT-SERVICE DECISION PENDING; PRODUCTION MOVE PENDING | 11 direct sideband-controller tests and all 90 `test_voice*.py` tests pass; changed Python compiles; capability source is guarded against direct `httpx`, loopback URL, and `client.post` use; production and candidate retain identical 149-route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362` |
| Synthetic canary | Active timer | voice operations | KEEP | Synthetic-only data and clear alert ownership |

## Work Runner

| Component | Verified current state | Target owner | Disposition | Gate |
|---|---|---|---|---|
| SeeBx job request/approval contracts | Not yet connected to cleaned backend | work capability | BUILD | Versioned contracts, explicit actor/target/tool authority |
| Work Runner sandbox manager | Separate server and repository | Work Runner | KEEP + CONTINUE | Disposable frontend/backend copies, no production credentials/data |
| Queue/scheduler | Not selected | work adapter | PILOT Supabase Queues/`pgmq`; Temporal deferred | Immutable job/receipt contracts first; failure/idempotency/load test before selection |

## Database schema disposition summary

| Current database/schema | Current direct or indirect use | Disposition |
|---|---|---|
| old `public` | chat, threads, attachments, telemetry, voice lease | MIGRATE to clean platform database |
| old `chat_history_private` | live deletion functions | MIGRATE after outbox decoupling |
| old `chat_integrity` | response attestation/snapshot | MIGRATE |
| old `trusted_web` | live search cache/audit/transcripts; the candidate governance audit shows the login role `brains_app` still owns the schema and three internal tables lack RLS | MIGRATE through a NOLOGIN owner, least-privilege grants, and forced-RLS transcript parity |
| old `lifeswitch_usage` | live forced-RLS usage ledger; candidate `c6285574` gives its writer one adapter owner and retires disconnected reporting code | MIGRATE, likely rename platform usage |
| old `ai_operations` | live admin incident schema; candidate batch 15 moves all six query/transaction effects and capability-session settings behind `adapters.ai_operations_postgres`, leaving the capability SQL-free while preserving inspector/manager authority and HTTP contracts | MIGRATE INTACT into the clean platform database after disposable parity proof |
| old `catalog_dev` | duplicate of isolated catalog; database-aware candidate audit `fdffcbe0` proves zero candidate runtime consumer across all 20 relations/functions and freezes 14 tables/1,711 rows | CONSOLIDATE into isolated catalog after exact parity and recovery proof |
| old `user_settings` | production router unmounted; exactly 1 preference row plus 19 compilation candidates; canonical candidate rebuilt | MIGRATE INTACT into the clean platform database after disposable parity proof |
| old `memory_ingest_private` | seven tables/1,216 rows; production chat-clear functions still make 12 objects database-internal-reachable. Candidate `0ee2b9fc` passed a complete 794-object disposable forward audit with zero reachable legacy-ingest dependencies and zero baseline blockers, then restored the exact 210-table baseline and deleted the temporary database | DISPOSABLE DECOUPLING PROVEN; production migration remains unapplied and separately approval-gated before final backup/retirement |
| old `memory` | 160 tables/35,118 rows plus six views; audit `fdffcbe0` proves zero candidate runtime consumer and classifies 566 relations/functions unproven | FINAL BACKUP/RESTORE, Zep/attestation proof, then retire with exact fail-closed package |
| five old Vantage schemas | removed from production after exact dump, temporary restore, count verification, and manifest receipt | RETIRED WITH RECOVERY EVIDENCE |
| five unmounted legacy public tables | `chat_messages`, `chat_sessions`, `feedback_signals`, `vantage_answer_trace`, and `vs_profiles`; 1,260 total rows; no candidate SQL caller or database dependency | FINAL BACKUP/RESTORE, then retire with exact non-cascading package |
| three old Forms tables | `vb_form_entries`, `vb_form_templates`, and `vb_form_versions`; 338 total rows; only the owner-bound migration utility references them | MIGRATE valid owner-bound rows to isolated LifeSwitch Forms, QUARANTINE invalid-owner rows, verify rollback/reconciliation, then RETIRE source tables |
| isolated `lifeswitch_*` schemas | live canonical domain data | KEEP |
| isolated `catalog_dev` | live canonical domain catalog candidate | KEEP and become sole catalog |

## Ledger maintenance rule

No component is deleted because it looks old. A retirement row advances only
after static imports, mounted routes, frontend callers, SQL/functions, services,
timers, environment settings, data retention, backups, tests, and rollback are
all explicitly resolved. Newly discovered components are added here before any
mutation batch is approved.

## 2026-08-21 retirement-readiness candidate update

- The clean-backend preflight is now v2 and requires a separate, write-denied `lifeswitch_retirement_auditor`; it explicitly proves that the `brains_app` application identity remains denied access to legacy private queues. The `sage` role remains a separately approved administrative execution identity and is never accepted as either application or inspection authority.
- The attestation quarantine now requires a mode-0600 AWS Secrets Manager custody receipt bound to the exact key fingerprint and immutable secret version; no secret value is persisted.
- The legacy-memory retirement SQL is executable only after every migration, queue, dependency, backup, restore, quarantine, and custody assertion passes. The package remains unapplied and unauthorized for production.

## Candidate verification evidence (2026-08-21)

- Base candidate commit: `dcf7b2092800b3d875a72fdd9d1c264b5f649c05`; production was not edited.
- Key-custody schema SHA-256: `442ae5d7006e860cd4a5eab999ee268ab8f6536d8625d4d2c52c2fbf3f66c1ce`.
- Reconciliation tool SHA-256: `cf35c1dd0885ae11c05d1e44e347b7f85193758f59e77193b8911a65e8c7028b`.
- Clean-backend preflight SHA-256: `87e8a861de1734aa8adb37a607c61172e66858eec764503f82d32f24e9d21e90`.
- Executable retirement SQL SHA-256: `00a82f89c47c68002cae61546adafcf5264b3507f2f296dd22829d09cf1f19e3`.
- Focused custody/preflight/retirement tests: 17/17 pass.
- Full backend suite in the verified pinned Python 3.12/Pydantic 2.12.3 runtime: 1,235/1,235 pass.
- Disposable PostgreSQL 16 positive execution: exact 160-table `memory`, 7-table `memory_ingest_private`, and 190/32/158 attestation fixture passed every assertion; only the two legacy schemas were dropped; Zep outbox and both chat-clear functions remained.
- Disposable PostgreSQL 16 negative execution: an external `public` view produced `external legacy dependencies remain`; the transaction aborted and both legacy schemas and the outside view remained.
- Package hashes, Python compilation, and `git diff --check` pass. No AWS secret, live database role/grant, migration, schema, service, frontend, or production source was changed.

- Candidate security boundary `20260821_legacy_memory_retirement_security_boundary_v1` corrects the application identity to `brains_app`, keeps `sage` administrative-only, defines a no-membership/read-only auditor that can execute only a content-free count function, and freezes exact read-only AWS Secrets Manager recovery policies. Production database roles, functions, secrets, and IAM resources remain unchanged and separately authorization-gated.
- Security-boundary verification: 17 focused tests and the complete 1,242-test backend suite pass in the pinned Python 3.12/Pydantic 2.12.3 runtime. A disposable PostgreSQL 16 execution proved read-only-by-default, exact six-count evidence, denial of chat-text reads and writes, and clean rollback. The offline package/hash/policy verifier passes. AWS Access Analyzer validation remains pending a fresh SSO session; no AWS resource was created or changed.

- Candidate conversation response connection boundary: the `/response/query` capability no longer imports `asyncpg`, opens, configures, or closes PostgreSQL directly. The composition root injects a named `PostgresConnectionProvider` retaining the exact 90-second command timeout and one shared assistant-preferences repository; actor verification and all fail-closed request checks still occur before connection acquisition. Production remains unchanged. Verification passed 36 focused tests and the complete 1,243-test backend suite in the hash-pinned Python 3.12 runtime. The clean parent and candidate both expose 138 routes and 124 OpenAPI paths with identical route-table SHA-256 `dacb3665272ec6720fb477340c9d84d977a6af55fae4f061972526eabe10353d` and OpenAPI SHA-256 `0c98736818a52d53797460c1f207f014d9882af64400363ae1fd69c21f54b46a`.

- AWS policy validation checkpoint: Access Analyzer returned zero findings for the source-role identity, recovery-role identity, and recovery-role trust policies. Secrets Manager correctly refused pre-creation resource-policy validation because the exact proposed recovery-role principal does not exist yet; both that role and the target secret are confirmed absent. Revalidate the secret policy after creating the role and before attaching the policy. No AWS resource changed.
- Candidate telemetry database boundary: one injected `PostgresTelemetryRepository` now owns all three telemetry connection, codec, transaction, RLS actor, SQL, and close effects. The capability owns only request validation and response shaping. Production remains unchanged pending full verification and a separate release. Verification passed 21 focused tests and the complete 1,247-test backend suite in the hash-pinned Python 3.12 runtime. Parent and candidate both expose 138 routes and 124 OpenAPI paths with unchanged route-table SHA-256 `dacb3665272ec6720fb477340c9d84d977a6af55fae4f061972526eabe10353d` and OpenAPI SHA-256 `0c98736818a52d53797460c1f207f014d9882af64400363ae1fd69c21f54b46a`.

- Candidate Nutrition Log database boundary: `capabilities.nutrition.logs` retains identity, validation, delegation policy, totals, serialization, and stable HTTP mapping but contains no direct PostgreSQL call, transaction, schema interpolation, or connection lifetime. `adapters.lifeswitch_nutrition_log_postgres` owns the exact People permission lookup, four read projections, eighteen write statements, existing atomicity boundaries, validated schema identifiers, and guaranteed close. All 23 normalized SQL hashes match the parent exactly. Twenty-two focused tests and all 1,264 backend tests pass. The preserved-parent verifier reports 138 routes with matching route-table SHA-256 `dacb3665272ec6720fb477340c9d84d977a6af55fae4f061972526eabe10353d` and OpenAPI SHA-256 `44e8aa5f364f85aef4d4fb4fa2596af4ab3eb219a2a82e21d54fbd2768c71091`. Production is unchanged.

- Candidate Meals database boundary: all 13 retained meal-template SQL effects, the one existing item-update transaction, validated schema identifier, and connection lifetime move from `capabilities.nutrition.meals` into `adapters.lifeswitch_meals_postgres`. Actor/owner authority remains capability-owned; the update repository invokes a capability-supplied owner check at the same point inside the transaction before serving validation or update. All 13 normalized SQL hashes match the parent. Seven focused adapter/transaction tests and all 1,271 backend tests pass. The capability has zero direct database call nodes. The preserved-parent verifier reports 138 matching routes, route-table SHA-256 `dacb3665272ec6720fb477340c9d84d977a6af55fae4f061972526eabe10353d`, and OpenAPI SHA-256 `44e8aa5f364f85aef4d4fb4fa2596af4ab3eb219a2a82e21d54fbd2768c71091`. Production is unchanged.

- Candidate Meal Plans database boundary: all 14 retained SQL statements, the one existing item-update transaction, strict schema identifier, and connection lifetime move into `adapters.lifeswitch_meal_plans_postgres`. Owner authorization, legacy-catalog restrictions, UUID validation, and HTTP errors remain capability-owned; callbacks preserve authorization and serving-ID parsing at their exact points inside the transaction. All normalized SQL hashes match the parent. Eight focused tests and all 1,279 backend tests pass. The six Meal Plan handlers contain zero direct database calls, and the preserved-parent verifier reports identical 138-route and OpenAPI hashes. Production is unchanged.

- Candidate Foods database boundary: all 43 retained food, serving, and override SQL occurrences, three existing transaction boundaries, strict nutrition/catalog schema identifiers, and connection lifetime move into `adapters.lifeswitch_foods_postgres`. The eleven HTTP handlers retain identity, validation, USDA/provider policy, owner checks, serialization, and stable errors with zero direct database calls. A handler-by-handler verifier expands repository calls and proves exact normalized SQL parity with SHA-256 `77bf3b2e9de0affe86d4f22c6bd2a83c1a51012b8b8cd8c1938306d35a361524`. Thirteen focused tests and all 1,287 backend tests pass. The preserved-parent verifier reports identical 138-route, route-table SHA-256 `dacb3665272ec6720fb477340c9d84d977a6af55fae4f061972526eabe10353d`, and OpenAPI SHA-256 `44e8aa5f364f85aef4d4fb4fa2596af4ab3eb219a2a82e21d54fbd2768c71091`. Production is unchanged.

- Candidate Training My Exercises boundary: the three retained My Exercises handlers keep actor/owner checks, UUID and role validation, input normalization, serialization, and HTTP errors while `adapters.lifeswitch_training_exercises_postgres` owns all five SQL occurrences, the existing transaction, role-event append, strict schema identifier, and guaranteed connection close. Handler-expanded SQL parity SHA-256 is `dea2c431ba7b376a694300e018b0ddb478f5e48b5b7118e9d1ad97019a7115fa`. Thirteen focused tests and all 1,294 backend tests pass; the preserved-parent verifier reports identical 138-route, route-table SHA-256 `dacb3665272ec6720fb477340c9d84d977a6af55fae4f061972526eabe10353d`, and OpenAPI SHA-256 `44e8aa5f364f85aef4d4fb4fa2596af4ab3eb219a2a82e21d54fbd2768c71091`. Production is unchanged.

- Candidate Training stored-writer boundary: all six protected Training and Conditioning stored-function calls plus transaction-local actor binding move from the mixed FastAPI/database `capabilities.training.logs` module into pure `adapters.lifeswitch_training_writes_postgres`; `capabilities.training.write_errors` now owns the exact prior SQLSTATE-to-HTTP mapping, and no compatibility wrapper remains. All seven normalized SQL effects match the parent with SHA-256 `3f018a6617034f48d1eec392d88a0693ce1149ff50c0f09d4d2bd6cc9d49ddca`. Eleven focused tests and all 1,296 backend tests pass; the preserved-parent verifier reports identical 138-route, route-table SHA-256 `dacb3665272ec6720fb477340c9d84d977a6af55fae4f061972526eabe10353d`, and OpenAPI SHA-256 `44e8aa5f364f85aef4d4fb4fa2596af4ab3eb219a2a82e21d54fbd2768c71091`. Production is unchanged.

- Candidate Training Conditioning boundary: all ten retained Conditioning SQL effects, three existing transaction boundaries, strict Training schema identifier, nine handler connection lifetimes, and the separate delegated People-permission query move into `adapters.lifeswitch_training_conditioning_postgres` and `adapters.lifeswitch_training_access_postgres`. The nine HTTP handlers retain identity, owner/delegation policy, validation, normalization, serialization, and stable errors with zero direct database calls; the formerly missing internal `Request` dependency on the library handler is explicit without changing OpenAPI. Exact SQL parity covers all 11 effects at SHA-256 `3b34eb0c17773903a6fe8317bdf5bd72fdbc97de066f90656b9da426c329fcc2`. Twenty focused tests and all 1,302 backend tests pass; the preserved-parent verifier reports identical 138-route, route-table SHA-256 `dacb3665272ec6720fb477340c9d84d977a6af55fae4f061972526eabe10353d`, and OpenAPI SHA-256 `44e8aa5f364f85aef4d4fb4fa2596af4ab3eb219a2a82e21d54fbd2768c71091`. Production is unchanged.

- Candidate Training workout-sharing boundary: all 17 retained sharing SQL effects, the existing import transaction, strict Training schema identifier, and five handler connection lifetimes move into `adapters.lifeswitch_training_sharing_postgres`. The five HTTP handlers retain actor/owner authority, token generation and hashing, validation, serialization, and stable error mapping with zero direct database calls. The formerly missing internal `Request` dependency on the preview handler is explicit without changing OpenAPI. Exact normalized SQL parity SHA-256 is `30641ba9c88577376b9be6d888e990f5ba1b831e2559d711009190cd796d2f94`. Twenty-three focused tests and all 1,310 backend tests pass; the preserved-parent verifier reports identical 138-route, route-table SHA-256 `dacb3665272ec6720fb477340c9d84d977a6af55fae4f061972526eabe10353d`, and OpenAPI SHA-256 `44e8aa5f364f85aef4d4fb4fa2596af4ab3eb219a2a82e21d54fbd2768c71091`. Production is unchanged.

- Candidate Training templates/exercises/segments boundary: all 21 retained SQL effects, both existing transactions, strict Training schema identifier, and ten handler connection lifetimes move into `adapters.lifeswitch_training_templates_postgres`. Capability-supplied owner callbacks preserve authorization between parent lookup and child access; HTTP handlers retain actor/owner authority, role and idempotency validation, normalization, serialization, and stable errors with zero direct database calls. The workout-role contract test now follows SQL authority to the adapter while retaining route-contract assertions. Exact normalized SQL parity SHA-256 is `7f359426e314b19abf6347bbcf2ff6c586fce4a59e379491c13db908430dd417`. Thirty-three focused tests and all 1,320 backend tests pass; the preserved-parent verifier reports identical 138-route, route-table SHA-256 `dacb3665272ec6720fb477340c9d84d977a6af55fae4f061972526eabe10353d`, and OpenAPI SHA-256 `44e8aa5f364f85aef4d4fb4fa2596af4ab3eb219a2a82e21d54fbd2768c71091`. Production is unchanged.

- Candidate Training strength-session/set-log boundary: all eight retained SQL effects, all three existing transaction boundaries, strict Training schema identifier, and eight handler connection lifetimes move into `adapters.lifeswitch_training_sessions_postgres`. The eight HTTP handlers retain actor/owner and delegated-read policy, validation, intent normalization, serialization, immutable-child retirement, and stable error mapping with zero direct database calls. The protected stored-function writer remains the only write authority and is invoked inside adapter-owned transactions. Exact normalized SQL parity SHA-256 is `e2d72b4f49c2c61efafea0023b6f6c4960257ce2b00334a07a376e2bc3aaee89`. Thirty focused tests and all 1,328 backend tests pass; the preserved-parent verifier reports identical 138 routes and 124 OpenAPI paths, retaining route-table SHA-256 `dacb3665272ec6720fb477340c9d84d977a6af55fae4f061972526eabe10353d` and OpenAPI SHA-256 `44e8aa5f364f85aef4d4fb4fa2596af4ab3eb219a2a82e21d54fbd2768c71091`. Production is unchanged.

## Candidate LifeSwitch disposable-database evidence (2026-08-21)

- `scripts/verify_lifeswitch_disposable_restore_v1.py` is the single canonical
  isolated-LifeSwitch database verifier. It replaces the zero-caller live-only
  verifier and fails closed around a unique template0 clone.
- Run `20260822t0250z` matched 48 tables, 5,590 exact rows, schema/ACL/RLS/
  function/policy/extension manifests, five owners, 33 protected surfaces, and
  165 application-visibility comparisons; it denied three cross-owner writes
  and snapshot access, preserved catalog reads, and proved clone cleanup.
- The hash-bound evidence and explicit remaining gates are recorded in
  `docs/SEEBX_LIFESWITCH_DISPOSABLE_DATABASE_VERIFICATION_V1.md`. Production
  source, database, configuration, and services were unchanged.

## Candidate LifeSwitch verified-identity boundary (2026-08-21)

- The paired frontend already forwarded the original bearer, but 70 direct
  LifeSwitch domain checks and eight database-derived owner paths still relied
  on the raw actor-header helper. The candidate now routes all of them through
  `seebx.core.identity`, with eight database-owner callbacks bound only to an
  already verified actor.
- Zero raw actor-header helper references remain in the eleven retained
  Measurements, Nutrition, Plans, and Training capability modules. Raw-header-
  only requests are rejected before repository access in all four domain
  groups.
- Forty-six focused tests and the complete 1,354-test backend suite pass.
  Parent/candidate route and OpenAPI structures remain exact. The paired
  frontend passes four bearer/wiring and ten Plan-driven four-macro tests.
- Exact authority, evidence, and remaining end-to-end gates are recorded in
  `docs/SEEBX_LIFESWITCH_DOMAIN_IDENTITY_BOUNDARY_V1.md`. Production is
  unchanged.
