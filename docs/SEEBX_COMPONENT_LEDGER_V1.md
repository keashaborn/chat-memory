# SeeBx component ledger v1

Status: cleanup control ledger; no retirement authority

Evidence date: 2026-08-17 America/Chicago (2026-08-18 UTC)

Source commit: `49f9e60cf4321c8e42c359845c1a62a8c987614d`

## Dispositions

- **KEEP**: correct capability and ownership boundary; tighten as needed.
- **CONSOLIDATE**: necessary behavior split among overlapping paths.
- **EXTRACT**: retain generic logic while removing retired-package coupling.
- **REBUILD**: useful product capability whose implementation is unsafe or
  obsolete.
- **DECIDE**: product requirement or build-versus-buy decision is unresolved.
- **ARCHIVE**: preserve immutable evidence outside the active repository.
- **RETIRE**: proven unused, but deletion still requires a separate manifest,
  backup, authorization, and verification.

## Runtime and platform

| Component | Verified current state | Target owner | Disposition | Gate |
|---|---|---|---|---|
| `app.py` composition/root routes | Production remains 1,985 lines/36 direct routes; isolated candidate is 1,467 lines/32 direct routes | `main.py` plus capability routers | CONSOLIDATE | Move remaining SQL and product behavior out; preserve API contracts until frontend cutover |
| Supabase actor verification | Candidate `a20e9634` separates request identity/owner binding into `seebx.core.identity` and `seebx.core.ownership`, and issuer/JWKS/JWT verification into `seebx.adapters.supabase`; candidate `63e2e45a` adds one owner-independent request-actor verifier and makes all ten active app-owned thread operations require a verified Supabase bearer/header binding or active owner-bound voice-session lease before PostgreSQL ownership lookup. Verbal Sage candidate `c25502b` forwards the already-verified original bearer through all active thread proxy calls, ownership preflights, and the inspector active-thread lookup; the service token remains an infrastructure wall only | core identity/ownership plus provider adapter | CANONICALIZED IN PAIRED CANDIDATES; PRODUCTION MOVE PENDING | Fifty-three backend identity/conversation tests and four thread contract checks pass; 31 frontend thread/security checks pass; candidate TypeScript diagnostics exactly match the existing frontend baseline; the backend retains 149 routes and exact OpenAPI SHA-256 `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362`. Deploy backend and frontend only as one rollback-bound compatibility unit after authenticated end-to-end proof |
| `memory_actor_auth_v1.py` | Batch 02 candidate reduces it to a compatibility re-export; live callers use `seebx.core.identity` directly | `seebx.core.identity` | EXTRACTED; compatibility RETIRE pending | Candidate `fb252149`; prove dormant callers migrated before deleting wrapper |
| Platform PostgreSQL connection | Production is live on `127.0.0.1:5432`, database `memory`. Candidate `2497b9e7` gives all 16 formerly application-owned raw and owner-scoped connection lifetimes, canonical UUID actor-session setup, close-on-success/failure, and readiness probing one `seebx.adapters.postgres.PostgresConnectionProvider`; `app.py` contains no direct `asyncpg.connect`, connection close, actor-session SQL, or query call | shared platform PostgreSQL connection/session adapter | APPLICATION CONNECTION LIFETIME CONSOLIDATED; OTHER ADAPTER-LOCAL CONNECTIONS AND PRODUCTION MOVE PENDING | Twenty-seven focused unittest checks and 14 static thread-contract functions pass. Owner-session SQL and readiness SQL exactly match baseline `2a9aafb4` with SHA-256 `cb78da1ce4ffe257139f4aa15e740d45d5edbd13a061c2f2299bb47ca1940ea2` and `822ae07d4783158bc1912bb623e5107cc9002d519e1143a9c200ed6ee18b6d0f`; changed Python compiles; all direct routes audit with zero direct SQL/connection calls; production/candidate API parity remains 149 routes with route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362`; still migrate only required schemas/functions and prove exact row/ACL parity |
| LifeSwitch PostgreSQL connection | Live on `127.0.0.1:55433`, database `lifeswitch` | LifeSwitch domain PostgreSQL | KEEP | Remove fallback to `POSTGRES_DSN`; retain isolated credentials |
| Redis container | Running; no `REDIS_URL` in the live service, no live process connection, 203 keys/~1.07 MB | none unless a scheduled consumer is proven | RETIRE CANDIDATE | Inventory key types/TTL and every timer/script before a rollback-safe stop; retain only if a named capability needs it |
| Qdrant runtime surface in `app.py` | Imported/configured but no live caller; no service/container | none | RETIRE | Batch 01 removes code/health surface; later remove dependency/config after dormant artifacts are archived |
| OpenAI SDK client and chat adapters | Candidate `0a1a4ba2` moves the OpenAI-only SDK client, model allowlist, embeddings, and compatibility helpers to `seebx.adapters.openai`; candidate `7f68089f` moves the generic and current LifeSwitch request/response/Chat Completions contracts from `rag_engine` to `seebx.adapters.openai_chat` and `seebx.adapters.lifeswitch_openai_chat` with no wrappers; live runtime has no `VANTAGE_MODEL` setting | provider adapters | CANONICAL PLACEMENT COMPLETE; PRODUCTION MOVE PENDING | Key fingerprinting permits in-process credential rotation without plaintext cache keys; all changed Python compiles; the 754-test conversation/voice/usage set retains only two failures reproduced at pre-move `fb133121`; all 53 Zep tests and exact 149-route/OpenAPI parity pass; historical manifests retain the former paths unchanged |
| `requirements-ci.txt` and `pyproject.toml` | CI dependencies include retired systems; package metadata describes governed-memory successor | SeeBx application package | REBUILD | Inventory real runtime/test dependency sets before replacement |
| Git daily sync | Active timer/service root | operations | KEEP | Verify remote/branch policy and avoid competing writer paths |
| Legacy `eval_all_users.sh` cron | Runs daily at 03:00; reads absent Qdrant `memory_raw`; recent runs do no useful work | none | RETIRE CANDIDATE | Back up the exact crontab and logs, remove only the one line, then prove no replacement scheduler depends on it |
| Voice synthetic canary | Active hourly but failed because its request used a field intentionally rejected by the live TTS route | voice operations | KEEP + REPAIR | Candidate `e7005538` aligns it with server-owned `conversation_style`; deploy and run one bounded canary before clearing historical failure state |

## Conversation and memory

| Component | Verified current state | Target owner | Disposition | Gate |
|---|---|---|---|---|
| Conversation response router | Candidate `b56d0e84` moves the mounted route from `rag_engine/resse_response_router.py` to `seebx/capabilities/conversation/router.py`, moves its test module, and leaves no compatibility wrapper; the external route, request schema, operation ID, and summary remain byte-for-byte OpenAPI compatible | `capabilities.conversation` | CANONICAL PLACEMENT COMPLETE; ORCHESTRATOR CONSOLIDATION CONTINUES | 117 bounded tests have exactly the same two pre-existing legacy failures before and after; compilation plus exact 149-route and OpenAPI hash parity pass; next split provider/composition selection by declared role without changing the public API |
| Conversation composition | Candidate `5ed33124` moves both live implementations from `rag_engine/response_composition_root_v0_2.py` and `v0_4.py` to `seebx.capabilities.conversation.composition` and `.lifeswitch_composition`; the generic pipeline is `ConversationResponseComposer`, the domain stage is `LifeSwitchResponseStage`, and the full wrapper is `LifeSwitchConversationComposer`; candidate `7f68089f` places both OpenAI chat adapters under `seebx.adapters`; candidates `b9069fa7`, `6f6ef721`, `f7a5f7e2`, `f89319c8`, and `9438dc52` place prompt assembly, orchestration, policy, finalization, and inspection under the conversation capability; `6f1272a1` places the LifeSwitch response plan, binding, and provenance contracts there; no legacy wrapper remains | conversation capability | CANONICAL PLACEMENT + ROLE NAMES COMPLETE; DEPENDENCY EXTRACTION CONTINUES | Compilation passes; focused conversation tests and all 53 Zep tests retain behavior with exact 149-route/OpenAPI parity; next extract the remaining LifeSwitch context dependencies without changing response behavior |
| Conversation prompt assembly | Candidate `b9069fa7` moves the single 1,172-line pure prompt implementation from `rag_engine/prompt_assembler_v1.py` to `seebx.capabilities.conversation.prompt`, updates every active importer and path-verification test, and leaves no wrapper; provider/runtime side-effect exclusions remain enforced | conversation capability | CANONICAL PLACEMENT COMPLETE; PRODUCTION MOVE PENDING | 86 focused tests retain only two failures reproduced at pre-move `d78506ad`; the 787-test conversation/prompt/voice set retains only its two previously documented failures; all 53 Zep tests, changed-file compilation, and exact 149-route/OpenAPI parity pass; historical handoffs and frozen evidence retain the old path unchanged |
| Conversation orchestration and policy | Candidate `6f6ef721` moves the 886-line orchestration implementation from `rag_engine/response_orchestration_v0_2.py` to `seebx.capabilities.conversation.orchestration`; candidate `f7a5f7e2` moves the 1,518-line policy implementation from `rag_engine/response_policy_v0_2.py` to `seebx.capabilities.conversation.policy`; all active importers and path-sensitive tests were updated, no wrappers remain, and stale RESSE/unused ownership descriptions were corrected without changing versioned or hash-bound contract fields | conversation capability | CANONICAL PLACEMENT COMPLETE; PRODUCTION MOVE PENDING | The 83-test focused orchestration suite retains only its two baseline guided-reflection failures; the 382-test policy/dependency suite has the exact same result before and after the policy move; the 787-test broad suite retains only its documented baseline failures; all 53 Zep tests, changed-file compilation, and exact 149-route/OpenAPI parity pass |
| Conversation finalization | Candidate `f89319c8` moves generic V1 finalization to `seebx.capabilities.conversation.finalization` and independently governed LifeSwitch V3 finalization to `.lifeswitch_finalization`; every active importer is updated, the old paths are explicitly guarded absent, and no compatibility wrappers remain; the two versioned implementations stay separate so LifeSwitch binding/provenance cannot alter the generic contract | conversation capability | CANONICAL PLACEMENT COMPLETE; PRODUCTION MOVE PENDING | All 40 focused finalization, persistence, inspection, provenance, and composition tests pass before and after; all 53 Zep tests pass; changed files compile; production and candidate both expose exactly 149 routes with identical route and OpenAPI hashes; full historical discovery remains non-gating because its existing stale manifests and retired suites produce unrelated failures |
| Conversation inspection | Candidate `9438dc52` consolidates the V1/V2 generic inheritance chain into `seebx.capabilities.conversation.inspection` and moves LifeSwitch V4 to `.lifeswitch_inspection`; the unnecessary V2 module boundary and duplicate strict-model base are removed, all V1/V2/V4 wire versions and builders remain, every active importer is updated, and explicit guards keep all three legacy paths absent | conversation capability | CONSOLIDATED; PRODUCTION MOVE PENDING | The same 35 focused inspection/path tests pass before and after, with only the same stale test expecting retired `persist_finalized_response_v3`; all 53 Zep tests pass; changed files compile; production and candidate retain identical 149-route and OpenAPI hashes; 19 net lines are removed |
| LifeSwitch response contracts | Candidate `6f1272a1` moves the current hash-bound response plan, final-answer binding, and content-free provenance receipt from three version-named `rag_engine` modules to `seebx.capabilities.conversation.lifeswitch_plan`, `.lifeswitch_answer_binding`, and `.lifeswitch_provenance`; all active runtime, adapter, persistence, clone-test, and focused-test importers move with them, and explicit guards keep the three former paths absent | conversation capability | CANONICAL PLACEMENT COMPLETE; PRODUCTION MOVE PENDING | The exact same 60 focused tests pass before and after; all 53 Zep tests pass; changed Python compiles; production and candidate retain identical 149-route, route, and OpenAPI hashes; wire versions, model validators, manifest hashes, stored contract names, and historical Git evidence remain unchanged |
| LifeSwitch context preparation and restricted reads | Candidate `b3a6925b` separates the pure selection, validation, hashing, and preparation contract into `seebx.capabilities.conversation.lifeswitch_context`; restricted owner-bound PostgreSQL access into `seebx.adapters.lifeswitch_context_postgres`; and environment, pool, and lazy-session management into `seebx.adapters.lifeswitch_context_runtime`. Both mixed version-named `rag_engine` modules are removed without wrappers | conversation capability plus PostgreSQL/runtime adapters | CONTRACT/ADAPTER/RUNTIME SEPARATION COMPLETE; PRODUCTION MOVE PENDING | The exact 60 focused tests pass; all 53 Zep tests pass; changed Python compiles; all 11 SQL-bearing statements and the complete restricted-session class AST are unchanged; production and candidate retain identical 149-route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362` |
| Conversation usage persistence | Production has 671 content-free usage events through 2026-08-17; the table is forced-RLS and the live conversation router is its only Python writer. Candidate `c6285574` replaces two version-named writer entry points with one `seebx.adapters.usage_postgres.persist_openai_chat_usage`, deletes the unmounted admin aggregation/cursor subsystem, and removes three one-time clone-verification scripts with zero external callers; the former admin route was already retired and the frontend main branch retains only a stale permission-registry label | PostgreSQL usage adapter plus platform usage schema | CONSOLIDATED; PRODUCTION MOVE PENDING; FRONTEND LABEL RETIREMENT PENDING | Five direct writer tests, the actor-registry tests, 60 conversation/usage tests, all 53 Zep tests, compilation, and exact 149-route/OpenAPI parity pass; 164 lines replace 2,129 lines while preserving the table, records, writer/admin roles, forced RLS, migrations, append/idempotency/conflict behavior, and historical Git evidence |
| Obsolete LifeSwitch V1 generation | Candidate `7ae253e1` removes `openai_chat_request_v3.py`, `response_lifeswitch_integration_v1.py`, `lifeswitch_prompt_integration_v1.py`, and their two V1-only test modules; the immutable historical handoff remains, and the exclusive-cutover test keeps explicit anti-reintroduction names | none | RETIRED IN CANDIDATE; PRODUCTION MOVE PENDING | Static imports, mounted routes, operational files, services, and timers have zero callers; 1,460 lines are removed without a wrapper; current V2/V4 focused tests add no failures, all 53 Zep tests pass, the 679-test affected set has only two failures reproduced at pre-change `dc9ffbd0`, and exact 149-route/OpenAPI parity is retained |
| Conversation snapshot | Candidate `e89018de` removes the mixed `rag_engine/response_conversation_snapshot_v1.py` module and leaves no compatibility wrapper; immutable validation, budgets, and hash-bound construction are owned by `seebx.capabilities.conversation.snapshot`, while repeatable-read owner/thread SQL and transcript-attestation checks are owned by `seebx.adapters.conversation_snapshot` | conversation capability plus PostgreSQL adapter | CONTRACT/ADAPTER SEPARATION COMPLETE; PRODUCTION MOVE PENDING | 18 direct snapshot tests and 139 affected tests pass; all 45 current `test_zep*.py` tests pass; 36 changed Python files compile; all six normalized SQL statements retain SHA-256 `a8a50d6b4e3aec8b99f2aeed1f0a4128a77f72feddbac6196440cbffb708caa2`; snapshot JSON/hashes and the 149-route/OpenAPI surfaces exactly match production; the governed-provider module retains the same 10 stale baseline errors before and after |
| Zep provider/runtime | Production prompt mode is on; candidate `25be0d0a` gives `app.py` and the response router one `seebx.capabilities.conversation.zep_runtime` singleton/settings owner | conversation capability with a future narrow `adapters.zep` provider boundary | KEEP; SHARED RUNTIME EXTRACTED; PRODUCTION MOVE PENDING | 66 focused conversation/Zep/erasure/response tests, all 52 `test_zep*.py` tests, compilation, and exact 149-route/OpenAPI parity pass; three broader stale tests fail identically at pre-batch `dd701e14`; still prove authenticated owner/thread binding, outage, export/retention, deletion compensation, and provenance before deployment |
| Old successor-memory adapter | Live only for excluded/no-memory surfaces | no-memory context provider | EXTRACT then RETIRE | Replace with explicit `NoMemoryContext` contract |
| Governed-memory package | 56 files; 16 reachable through response/auth coupling, 40 dormant | none after extraction | EXTRACT then ARCHIVE/RETIRE | Zero live imports and migration/runtime callers; 10 provider-test errors at pre-adapter candidate `000fc2b2` still assert retired `memory_input`/`memory_application` fields and must be reconciled with retirement rather than treated as current adapter regressions |
| `memory_v1*` code and active-runtime manifest | 45 files/26,764 lines in `rag_engine` have no live roots; the so-called active-runtime manifest declares 39 `memory-v1-*` units, but the server now has zero installed `memory-v1-*` units and the manifest/verifier has only test/script consumers, with no live service or timer reference | none | ARCHIVE then RETIRE | Preserve the hash-bound manifest as historical evidence; retire it only through an explicit manifest after zero frontend and operational callers are reconfirmed; do not refresh its hashes to make obsolete architecture look current |
| Vantage code/routers | Unmounted/dormant | none | ARCHIVE then RETIRE | Confirm frontend `/vantage/query` caller removal and preserve required audit evidence |
| RESSE policy/evaluation code and compatibility vocabulary | The live router/composer implementation paths and class names are product-neutral in candidate `5ed33124`; dormant RESSE evaluation code remains, while external `ResseResponseRequestV1`, operation/runtime identifiers, logs, and `RESSE_CLASSIFIER_MODEL` are still compatibility surfaces | evaluation archive plus versioned API compatibility | ACTIVE IMPLEMENTATION RENAMED; ARCHIVE/RETIRE DORMANT; VERSION EXTERNAL CUTOVER | Preserve controlling evaluation evidence outside active imports; inventory every frontend, monitoring, environment, and stored consumer before replacing external names in one separately versioned cutover |
| Direct cards/Vantage routes in `app.py` | Mounted but return retired conflict responses | none | RETIRE after frontend cutover | Remove `/api/identity` and inspect callers first |
| Chat/thread routes | Live and PostgreSQL-backed. Backend candidate `63e2e45a` verifies Supabase or active voice-session authority before every active app-owned thread lookup; Verbal Sage candidate `c25502b` forwards the original bearer through the thread proxy boundary | conversation capability | KEEP + CONSOLIDATE | Move the remaining HTTP handlers into one authenticated conversation router; deploy the paired candidates only after authenticated end-to-end proof and preserve thread semantics |
| Conversation persistence, transcript integrity, and active-thread selection | Candidate `d247da49` makes `seebx.capabilities.conversation.persistence` a SQL-free validation contract; `seebx.adapters.conversation_persistence` owns the atomic response/search writes; `seebx.contracts.transcript_integrity` owns the pure hash-bound attestation; and `seebx.adapters.transcript_integrity` plus `.thread_selection` own their PostgreSQL effects. The two zero-runtime versioned persistence wrappers and their misleading test names are removed without wrappers; stored web source identifiers remain unchanged in `seebx.contracts.conversation` | conversation contracts plus PostgreSQL adapters | CONTRACT/ADAPTER SEPARATION COMPLETE; DUPLICATE WRAPPERS RETIRED; PRODUCTION MOVE PENDING | 68 focused tests, all 53 Zep tests, 67 search tests, and compilation pass; 17 security/effect AST nodes and all 15 normalized SQL statements are unchanged with SQL hash `e7206ecfd1b8885d835a9464603e29b6315568731688664b9089fe7db481287b`; production and candidate retain identical 149-route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362` |
| Chat history clear | Live; secure DB functions; hidden dependency on legacy memory outbox | conversation deletion service | CONSOLIDATE | Replace outbox dependency only after Zep deletion and replay semantics are tested |
| Full chat + Zep clear | Live | conversation deletion service | KEEP | Verify atomic/compensating behavior and owner isolation |
| Attachments | Candidate `988596bd` owns validation/rendering and response-time reads in the conversation capability/PostgreSQL adapter. Candidate `b1a6b3bd` moves all four CRUD endpoints, five CRUD statements, connection lifetime, and the shared strict JSON UUID type out of `app.py`. Candidate `7b6ed07e` moves both owner/thread-bound message-binding statements into the attachment adapter without changing transaction or replay/conflict semantics. Candidate `92745e3b` moves the complete ordered thread-history query—including trusted-web evidence and attachment metadata projection—to `adapters.conversation_history`; `app.py` now contains no `public.chat_attachments` SQL | conversation capability plus shared identifier contract and PostgreSQL attachment/history adapters | ATTACHMENT CONTRACT, RESPONSE READ, CRUD, MESSAGE-BINDING, AND THREAD-HISTORY DATABASE BOUNDARIES EXTRACTED; PRODUCTION MOVE PENDING | Sixteen focused history/attachment/active-thread tests pass. The normalized thread-history query exactly matches pre-batch SHA-256 `acd12e056b2834fa63e7626116d20a777d5b40fb715ee8e3ff7979aded42b748`; changed Python compiles; the candidate remains at 149 routes with route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362` |
| Thread metadata lifecycle | Candidate `6e1c6e51` moves eight owner-scoped statements for create, visible-list, manual rename, pin/unpin, archive, title-state read, title-transcript read, and compare-and-set automatic-title update into `seebx.adapters.conversation_threads`; candidate `c210e2d8` adds the owner-bound thread-authorization lookup to that same adapter; candidate `63e2e45a` requires independently verified request-actor authority before that lookup, paired with Verbal Sage bearer-forwarding candidate `c25502b`. Route validation, 404 behavior, response shapes, transaction scope for create-plus-active-selection, and model generation remain unchanged in the capability layer | conversation capability plus PostgreSQL thread adapter | THREAD DATABASE EFFECTS AND APPLICATION CONNECTION LIFETIME CONSOLIDATED; PRODUCTION MOVE PENDING | Fifty-three backend identity/conversation tests, four thread contract checks, and 31 frontend thread/security checks pass; candidate TypeScript diagnostics exactly match the frontend baseline. The original eight normalized statements exactly match candidate baseline `cbd8a5af`; the owner lookup exactly matches baseline `22fe578e` with SHA-256 `3b63b68642a47c1829d1cdf804ee8f87a225701ad62ca5cedc8f1cd9a4df5daf`; changed Python compiles; production/candidate API parity remains 149 routes with route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362` |
| User transcript persistence | Candidate `62b5178c` moves all seven `/log` statements plus thread creation/touching, submission idempotency, attachment replay/binding, and explicit commit/rollback into the existing `seebx.adapters.conversation_persistence` owner. The route retains request/authentication validation, HTTP error mapping, and response shape; candidate `2497b9e7` moves its connection acquisition, actor-session setup, and guaranteed close to the shared PostgreSQL provider | conversation capability plus canonical PostgreSQL conversation persistence adapter | ATOMIC USER-TRANSCRIPT TRANSACTION AND APPLICATION CONNECTION LIFETIME CONSOLIDATED; PRODUCTION MOVE PENDING | Twenty-one focused tests pass, including fresh writes, missing-thread creation, exact replay, conflicting reuse, attachment replay/binding/refusal, database failure, duplicate-key classification, route delegation, assistant/search persistence, and unchanged route-level idempotency. All seven normalized SQL statements exactly match baseline `526d4bf6`; changed Python compiles; `/log` contains no SQL or transaction ownership; production/candidate API parity remains 149 routes with route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362` |
| User export | Route exists but returns retirement conflict; frontend still calls it | privacy/export capability | REBUILD | Owner-scoped export contract, bounded data, audit, and frontend test |

## Search, retrieval, and catalog

| Component | Verified current state | Target owner | Disposition | Gate |
|---|---|---|---|---|
| Trusted-health search profile | Candidate mounts `seebx.capabilities.search.trusted_health`; provider, policy, registry, evidence, admission, and audit have one canonical owner | search capability | KEEP | Preserve approved domains, NCBI/ODS behavior, source evidence, and audit semantics through deployment verification |
| Search executor and shared manifest contract | Candidate owns plan, budget, authorization, and execution under `seebx.capabilities.search`; `bed831f5` moves the server-owned hashed `SearchCapabilityManifestV1` from `rag_engine` to `seebx.contracts.search` with no wrapper; shared language, OpenAI, transcript persistence, request identity, and voice-session authority use their canonical owners | search capability plus shared contracts | KEEP; SHARED BOUNDARIES AND CONTRACT EXTRACTED | 93 affected tests, compilation, a no-wrapper regression test, and exact 149-route/OpenAPI parity pass; preserve manifest bytes, server-owned budgets, and authenticated owner behavior through deployment tests |
| Search PostgreSQL effects | Candidate `7ffb6b82` leaves only content-free query hashing in `seebx.capabilities.search.audit`, keeps SQL-free settings/errors/HTTP behavior in `.runtime`, and moves the audit connection lifecycle, shared actor rate limit, audit start/finish, and metadata-only source evidence writes to `seebx.adapters.search_audit`; candidate `b2b6311f` moves persisted-search connection ownership to `seebx.adapters.search_transcript` and the active/unexpired ODS cache read to `seebx.adapters.search_cache`. The complete `seebx.capabilities.search` package now contains no direct PostgreSQL import, connection, fetch, or execution effect | search capability plus PostgreSQL search adapters | CONTRACT/ADAPTER SEPARATION COMPLETE; PRODUCTION MOVE PENDING | 147 search tests, all 53 Zep tests, focused endpoint/adapter failure and cleanup tests, and compilation pass; the normalized ODS query is unchanged with SQL hash `70c806103d1901fce7230535b38502cb9414e7d0b747c1692700a023abc4414f`; production and candidate retain identical 149-route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362` |
| Current-news search profile | Candidate expresses current news through the same provider, registry, evidence, admission, audit, and runtime boundaries | search capability policy profile | KEEP | Preserve freshness/source policy and exact public contract; do not recreate a separate engine |
| Legacy search-owned `rag_engine` modules | Candidate removes or moves 15 router, authorization, plan, budget, evidence, audit, provider, policy, registry, contract, NCBI, and ODS modules; `bed831f5` removes the final shared manifest path and no candidate caller imports it | none | RETIRE CANDIDATE COMPLETE | Keep Git rollback; verify deployment import graph, routes, authenticated behavior, ODS timer, and stored audit records before production retirement is accepted |
| Legacy RAG/vector modules | Mostly dormant with Qdrant/Vantage dependencies | none or offline evaluation | ARCHIVE then RETIRE | Generated import graph and no runtime settings/services |
| `seebx/capabilities/catalog/routes.py` | Live against old platform `catalog_dev` | catalog capability in LifeSwitch database | MIGRATE + CONSOLIDATE | Repoint after row/function parity; do not keep two writable catalogs |
| LifeSwitch nutrition catalog access | Live against isolated database `catalog_dev` | same catalog capability | KEEP | Becomes sole catalog after callers cut over |

## LifeSwitch domain capabilities

| Component | Verified current state | Target owner | Disposition | Gate |
|---|---|---|---|---|
| Nutrition router | Live; 17 routes/1,503-line closure plus overlapping catalog paths | `capabilities.nutrition` | KEEP + SPLIT | Separate log, foods, meal plans, and catalog contracts |
| Training router | Live; 41 routes/2,956-line closure | `capabilities.training` | KEEP + SPLIT | Separate sessions, exercises, plans, and analytics without duplicate endpoints |
| Measurements | Live | `capabilities.measurements` | KEEP | Retain owner checks and isolated PostgreSQL |
| Plan/domain context | Live across multiple readers/adapters | `capabilities.plans` | CONSOLIDATE | One read model and one write authority; retire legacy fallback |
| LifeSwitch snapshots | Installed schema; currently empty | plans/recovery if required | DECIDE | Prove product caller and retention need |
| Agentic plan observation | Live import path | plans read model | CONSOLIDATE | Move SQL behind domain adapter; no response-router SQL |
| Duplicate nutrition batch routes | Live candidate duplication | nutrition service | CONSOLIDATE | Frontend caller trace and response equivalence |

## Preferences, forms, administration, and archive

| Component | Verified current state | Target owner | Disposition | Gate |
|---|---|---|---|---|
| Assistant response preferences | Backend router is owner-aware but unmounted; frontend calls it | preferences capability | RESTORE through refactor or RETIRE | Determine intended product behavior; do not remount implicitly |
| `user_settings` schema | Two empty tables in old database | platform preferences | MIGRATE/REBUILD if retained | Current authenticated API and schema contract |
| Forms router | Unmounted; trusts request-provided owner ID | forms capability | REBUILD or RETIRE | Never remount current implementation; require verified identity/RLS |
| Frontend forms surfaces | Active callers exist | forms capability | DECIDE | Product decision plus migration/removal UX |
| Telemetry router | Live and large/custom | observability capability | CONSOLIDATE behind OpenTelemetry contract | Define redacted trace/request/job correlation; select storage/alert backend separately |
| AI operations incidents | Live routes; four empty tables | operations | KEEP or REPLACE | Confirm consumer and alert delivery; product comparison |
| Admin memory workbench/health routes | Mounted retirement responses | none | RETIRE | Remove frontend/admin callers and historical tests first |
| Archive/document library | Separate planned capability; not chat memory | archive capability | DECIDE | Product evaluation for storage, extraction, mapping, export, and deletion |

## Voice

| Component | Verified current state | Target owner | Disposition | Gate |
|---|---|---|---|---|
| Voice session lease | Candidate `381704fb` removes the legacy combined router: `seebx.core.voice_identity` owns request/session authority, `seebx.adapters.voice_session` owns PostgreSQL lease operations, and `seebx.capabilities.voice.session` owns the unchanged HTTP contract | voice capability | EXTRACTED; PRODUCTION MOVE PENDING | 64 full voice tests, 41 authority-reference tests, 298 search/trusted-web tests, compilation, exact 149-route/OpenAPI parity, and all eight normalized lease SQL statements pass/match |
| Voice-language contract | Candidate moves the shared catalog, validation, request header, response instruction, and transcription prompt from `rag_engine` to `seebx.contracts.voice_language` | shared contracts | EXTRACTED; PRODUCTION MOVE PENDING | Candidate `f8b73bf1`; 111 direct voice/response tests, 154 search tests, 62 voice tests, and exact route/OpenAPI parity pass; production still requires reviewed deployment |
| Shared voice observability and realtime preview state | Candidate `92adf9e3` moves content-free request correlation to `seebx.core.voice_observability` and the provider-free in-memory preview registry/state machine to `seebx.capabilities.voice.realtime_session`; every active importer uses the canonical owner and neither legacy path remains as a wrapper | core observability plus voice capability state | CANONICAL PLACEMENT COMPLETE; PRODUCTION MOVE PENDING | Both moved source files are byte-identical with hashes `7452a22d1cff711c256adf5f852b9a345a9bdf44a43519a2f45b0ddbdbaa49ca` and `6926ebb7240e033533f3fdc80abdca4f99bd261f60ed1e1b7b6174862b42e995`; 64 focused tests, all 66 `test_voice*.py` tests, compilation, and exact 149-route/OpenAPI parity pass |
| Voice transcription | Candidate `f3889883` mounts `seebx.capabilities.voice.transcription` directly with no legacy wrapper; owner/lease authority, audio/language limits, confidence calculation, and public HTTP semantics remain capability-owned, while `seebx.adapters.openai_transcription` owns model/URL/key configuration, safety-identified OpenAI HTTP, timeout/network handling, provider request identity, and response decoding | voice transcription capability plus OpenAI transcription adapter | CAPABILITY/PROVIDER SEPARATION COMPLETE; PRODUCTION MOVE PENDING | 17 focused tests and all 72 `test_voice*.py` tests pass; changed Python compiles; pure helper and provider-configuration ASTs are exact; production and candidate retain identical 149-route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362` |
| Voice synthesis/TTS | Candidate `03a4657f` mounts `seebx.capabilities.voice.synthesis` directly with no legacy wrapper; actor/session authority, request/text limits, server-owned model/voice/style, media contract, and public error mapping remain capability-owned, while `seebx.adapters.openai_tts` owns URL/key/default-voice configuration, safety-identified OpenAI HTTP, request payload, provider identity, streaming-resource lifetime, and provider failures | voice synthesis capability plus OpenAI TTS adapter | CAPABILITY/PROVIDER SEPARATION COMPLETE; PRODUCTION MOVE PENDING | 19 focused tests and all 79 `test_voice*.py` tests pass; changed Python compiles; pure capability and provider-configuration ASTs are exact against the former source hash `13c4198f51abe71b77f1c7ba02c70cd7444f1c6b1b50de9203e0494809e9f364`; success, upstream-error, and timeout cleanup are directly tested; production and candidate retain identical 149-route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362` |
| Realtime voice preview | Candidate `c9a552df` mounts `seebx.capabilities.voice.realtime_preview` directly with no legacy router wrapper; owner/lease checks, SDP limits, session lifecycle, commit/events/delete semantics, and public error mapping remain capability-owned; `seebx.adapters.openai_realtime` owns provider negotiation and `seebx.adapters.voice_realtime_config` owns secret loading. Candidate `64cd487e` moves the sideband controller to `seebx.capabilities.voice.realtime_sideband` and gives `seebx.adapters.openai_realtime_sideband` sole ownership of OpenAI WebSocket effects. Candidate `fd360921` gives the same-process loopback URL, client lifetime, timeout policy, three exact governed-turn paths, and HTTP exception translation to `seebx.adapters.voice_governed_turns`; the capability retains payload, authority, ordering, persistence validation, and public error semantics but no longer performs direct network effects | voice realtime capability plus OpenAI realtime/config/sideband and governed-turn transport adapters | PROVIDER EFFECT SEPARATION COMPLETE; GOVERNED LOOPBACK CONTAINED; DIRECT-SERVICE DECISION PENDING; PRODUCTION MOVE PENDING | 11 direct sideband-controller tests and all 90 `test_voice*.py` tests pass; changed Python compiles; capability source is guarded against direct `httpx`, loopback URL, and `client.post` use; production and candidate retain identical 149-route hash `2273bc5354a98d98994f12dcbae8d6c5175f67de10b052d9987bb9bb9178dfb5` and OpenAPI hash `efc724706c24741b734680767ea8350b57555f59c34b01919bdd844411694362` |
| Synthetic canary | Active timer | voice operations | KEEP | Synthetic-only data and clear alert ownership |

## Work Runner

| Component | Verified current state | Target owner | Disposition | Gate |
|---|---|---|---|---|
| SeeBx job request/approval contracts | Not yet connected to cleaned backend | work capability | BUILD | Versioned contracts, explicit actor/target/tool authority |
| Work Runner sandbox manager | Separate server and repository | Work Runner | KEEP + CONTINUE | Disposable frontend/backend copies, no production credentials/data |
| Queue/scheduler | Not selected | work adapter | PILOT Supabase Queues/`pgmq`; Temporal deferred | Immutable job/receipt contracts first; failure/idempotency/load test before selection |

## Database schema disposition summary

| Current database/schema | Current direct or indirect use | Disposition |
|---|---|---|
| old `public` | chat, threads, attachments, telemetry, voice lease | MIGRATE to clean platform database |
| old `chat_history_private` | live deletion functions | MIGRATE after outbox decoupling |
| old `chat_integrity` | response attestation/snapshot | MIGRATE |
| old `trusted_web` | live search cache/audit/transcripts | MIGRATE |
| old `lifeswitch_usage` | live forced-RLS usage ledger; candidate `c6285574` gives its writer one adapter owner and retires disconnected reporting code | MIGRATE, likely rename platform usage |
| old `ai_operations` | live admin code; no estimated rows | DECIDE/possibly migrate |
| old `catalog_dev` | live catalog router; duplicate of isolated catalog | CONSOLIDATE into isolated catalog |
| old `user_settings` | router unmounted; empty | REBUILD/MIGRATE only if preferences retained |
| old `memory_ingest_private` | two terminal outbox rows; indirect deletion-function dependency | DECOUPLE, archive evidence, retire |
| old `memory` | 160 tables; no verified live SQL reference | ARCHIVE then RETIRE after zero-dependency proof |
| five old Vantage schemas | no verified live Python reference; historical data present | ARCHIVE then RETIRE after exact backup/approval |
| isolated `lifeswitch_*` schemas | live canonical domain data | KEEP |
| isolated `catalog_dev` | live canonical domain catalog candidate | KEEP and become sole catalog |

## Ledger maintenance rule

No component is deleted because it looks old. A retirement row advances only
after static imports, mounted routes, frontend callers, SQL/functions, services,
timers, environment settings, data retention, backups, tests, and rollback are
all explicitly resolved. Newly discovered components are added here before any
mutation batch is approved.
